# CR-074 方案评审纪要

| 文档信息 | 内容 |
|---------|------|
| 版本 | v1.0 |
| 状态 | 已归档（评审结论：**有条件通过**——4 处必修项修订后方可开工） |
| 评审人 | 项目发起人（本人执行核对与评估） |
| 评审对象 | [CR-074-C档体验升级.md](../开发/实施方案/CR-074-C档体验升级.md) v1.0（评审时点版本；5 条建议项已落 v1.1） |
| 归档人 | AI 会话（sess_33dabe90，按发起人口述报告落档） |
| 最后更新 | 2026-09-19 |

> ⚠️ **归档附注（AI 代录）**：本纪要按发起人送达的评审报告原文归档。**「必修项（确认前应改方案）」一节的 4 条清单在送达消息中未随文送达（该节为空）**，仅总评留有分类概述（"1 个安全口径缺失、2 处事实性瑕疵、1 处与前端现状不符"）——**已请发起人补发原文**；补发前方案维持「待确认」、**不视为可开工**。

---

## 总评

**有条件通过（修改 4 处后可确认开工）**。方案整体质量高：范围克制、「不做」清单诚实、依赖方向设计合规、编号与引用绝大多数经核实无误、流程姿态正确（三项门槛均触 → 先方案后代码，台账 v1.78 已先行登记为「待确认、未动代码」，与发起人选定 C 档的指令有据）。发现的问题集中在 **1 个安全口径缺失、2 处事实性瑕疵、1 处与前端现状不符**，均为方案文档内的小修，不动摇方案结构。

## 经核实无误的关键引用

- **CR-074 编号合法**：台账 v1.78 已登记（CR-073 已占用），登记在前、方案状态「待确认」、未动代码；
- **PRD F-SOC-002「关注用户」确为 P1**（产品需求文档.md:81），方案把它定性为「范围决策、待发起人确认」（D1）是诚实的；
- **错误码**：`2009` 空闲可用；`2007 = USER_NOT_FOUND(404)`（ResultCode.java:25）用于「关注目标不存在」语义恰当；复用 `4001 / 4002` 正确；
- **`V4` 空闲**（现仅 V1~V3），`posts` 表无 `view_count` / `cover_url`，无 `follows` 表；
- **快照下一版 v1.14**（现行 v1.13）；「非纯新增、`ApiResponseVoid` 可能重排、沿用结构化比对判据」表述与 v1.12 立的判据一致；
- **`GET /posts/{id}` 是 `@PublicEndpoint`**（PostController.java:106）——「匿名按 IP 计数」的前提成立；且**无编辑帖子端点**，「封面发帖时一次提取」不存在编辑重算歧义；
- **依赖方向合规**：`follows` 归 account 上下文、forum web 编排两上下文 application——符合「跨上下文只调对方 application」，不触 G 系列守护测试；`PostListItem.vue` 存在；
- **回滚走前向迁移**符合「迁移不可改」约定；「ADR-006 口径不动」「关注不新增通知 type（CR-066 先例）」均与在档事实一致。

## 必修项（确认前应改方案）

（⚠️ 此节 4 条清单在发起人消息送达时缺失，待补发后由 AI 会话补录于此。）

## 建议项（不阻塞确认，实施时补）→ **已于方案 v1.1 全部落实**

1. **Redis 键约定**：阅读数去重键须走 `RedisKeys.of(...)` 前缀（约定强制，`RedisKeyNamespaceTest` 有断言），§3 值得点名一句。→ ✅ §3 已点名；
2. **契约同步义务不全**：步骤④只列了快照再生成；按约定还应同步**技术方案 §5**（错误码 2009 一行）与**契约 README**（v1.14 登记），建议写入步骤⑦。→ ✅ 步骤④已补；
3. **吞错应留痕**：计数失败「吞错」但按日志分档精神应记 1 行 WARN，否则静默失败不可见（R3 只定了不影响主流程，没定可观测性）。→ ✅ §2 已补 WARN 要求；
4. **验证缺「回归面」**（模板 §5 四项之一）：至少列三条——HomeView 改动对最新/热门切换回归、`PostSummaryVo` 加字段对搜索/热榜/收藏等既有消费方向后兼容、封面提取不动 `MarkdownRenderer`（`MarkdownRendererTest` 不受影响）。→ ✅ §5 已补三条；
5. **§7 与模板冲突**：模板 §7 是「实施结果（实施后追加）」，本方案把 §7 用作风险表，实施后追加结果时会错位——建议实施结果挂 §8。篇幅约 2 页也略超「一页以内」，可接受。→ ✅ 实施结果挂 §8（占位），§7 保留风险表并加对齐说明。

## 流程提醒（方案之外）

工作区当前叠着 **CR-072 + CR-073 的未提交改动**（HomeView / PostDetailView / PublishView / client.ts / forum.ts 等）。CR-074 实施会继续改同一批文件——建议**先把 072/073 提交再开工 074**，否则三个 CR 无法按「凭证 = commit message 写 CR 号」的约定分开对账。

## 结论

方案可实施性判断：**改完上述 4 处必修即可请发起人确认**。全部是小改（方案内补几行文字），不需要重写。
