# 开发测试账号清单

| 文档信息 | 内容 |
|---------|------|
| 版本 | v1.0 |
| 状态 | 已生效（**仅限本机开发库**） |
| 维护人 | AI 会话（[CR-054](../变更日志/变更台账.md#cr-054)） |
| 最后更新 | 2026-09-15 |
| 数据来源 | [`backend/scripts/data/D001__seed_dev_roster_45.py`](../../backend/scripts/data/D001__seed_dev_roster_45.py) 确定性生成，2026-09-15 已灌入本机 `campuslink` 库 |

**一句话**：本机开发库里已有 **45 个可直接登录的模拟学生账号**，登录方式见 §2，账号见 §3。

> ⚠️ **全合成身份，不含任何真实学生数据**。学号取保留段 `888800001`~`888800045`（真实名册不可能出现）、邮箱域 `dev.campuslink.local`（不可投递）、名册批次 `dev-sim-45`，三者合起来保证这批数据**一眼可辨、可整批清理**。本清单**不得**用于任何共享 / 预发 / 生产环境，也**不得**据此推断真实学号规则。

## 1. 这批数据是什么、不是什么

| 是 | 不是 |
|---|---|
| 开发期联调用的**模拟学生**：45 行名册 + 45 个已注册账号 | ❌ **不是学籍名册方案落地**——B1 / B2 真实名册仍是未到位的外部依赖（见[下一步行动清单](../下一步行动.md)） |
| 走**真实生产链路**产生的数据（名册导入 + 学籍核验 + 注册），加密 / 哈希 / 审计日志与正式数据同构 | ❌ **不是绕过安全机制的后门**——脚本只做编排，不复刻 HMAC 与 AES-GCM、不碰 `APP_HASH_KEY` / `APP_CRYPT_KEY` |
| 可整批清理（见 §5） | ❌ **不是内容数据**——这批账号没有发过任何帖子 / 回复，论坛侧仍需按需造内容 |

## 2. 怎么登录（**本系统没有密码**）

登录 = **邮箱 + 6 位验证码**，全流程只有这一步凭据：

1. 打开 `http://localhost:5173/login`，停在「登录」Tab；
2. 填入 §3 里任一**登录邮箱**（如 `dev-stu-01@dev.campuslink.local`）；
3. 点「获取验证码」；
4. 验证码填 **`123456`** —— 这是开发期固定码（`campuslink.captcha.fixed-code`，后端启动时打 WARN），**生产必须留空**，留空后须从后端日志捞码（`CODE_SENDER_MODE=log` 下行格式 `[DEV] captcha for <邮箱> => <6位码>`）。

**关于"密码"**：本系统从设计到实现都**没有密码字段**（V1 / V2 迁移无 `password` 列，`src/main/java` 零引用）。发起人原话里的"账号和密码"，落到本系统即"邮箱 + 验证码"，故本清单**不设密码列**。若确实需要密码登录，那是 [PRD](../需求/产品需求文档.md) F-ACC-001 的**需求变更**，须另开 CR 走流程，不能由本清单代答。

**两个会撞上的限制**（都属既有设计，非缺陷）：

- **验证码重发间隔 60 秒**（`campuslink.captcha.resend-interval-seconds`）：60 秒内重复点「获取验证码」返回 `2002`，且**原验证码已被上一次校验消耗**，此时登录会得到 `2005 邮箱或验证码错误`。等过 60 秒重新获取即可；
- **学籍核验 10 次/小时/IP**（`campuslink.verify.ip-hourly-limit`）：只影响**注册**链路，不影响登录。批量灌数据时用 `VERIFY_IP_HOURLY_LIMIT=<n>` 临时抬高（见 §4）。

### 管理员账号（既有，非本批产生）

| 用途 | 邮箱 | 登录方式 | 角色 |
|---|---|---|---|
| 名册导入 / 后台操作 | `admin@campuslink.local` | 同上（验证码 `123456`） | `SUPERADMIN` |

由 `DevAdminAccountSeeder` 在 `campuslink.roster.bypass=true` 时种子化，**无学号、不走学籍核验**。⚠️ 该种子**只在 bypass=true 时创建**：若把 bypass 关掉再清库，管理员不会自动重建。

## 3. 45 个模拟学生账号

统一属性：**年级** `模拟2024级`、**专业** `模拟-计算机科学与技术`、**角色** `USER`、**名册批次** `dev-sim-45`（全部已注册，名册行 `used_user_id` 均已占用）。

| 序号 | 学号 | 姓名 | 登录邮箱 | 昵称 |
|---|---|---|---|---|
| 1 | `888800001` | 赵子涵 | `dev-stu-01@dev.campuslink.local` | 模拟学生01 |
| 2 | `888800002` | 赵雨桐 | `dev-stu-02@dev.campuslink.local` | 模拟学生02 |
| 3 | `888800003` | 赵浩然 | `dev-stu-03@dev.campuslink.local` | 模拟学生03 |
| 4 | `888800004` | 赵思远 | `dev-stu-04@dev.campuslink.local` | 模拟学生04 |
| 5 | `888800005` | 赵嘉懿 | `dev-stu-05@dev.campuslink.local` | 模拟学生05 |
| 6 | `888800006` | 赵若曦 | `dev-stu-06@dev.campuslink.local` | 模拟学生06 |
| 7 | `888800007` | 赵沐辰 | `dev-stu-07@dev.campuslink.local` | 模拟学生07 |
| 8 | `888800008` | 赵书瑶 | `dev-stu-08@dev.campuslink.local` | 模拟学生08 |
| 9 | `888800009` | 赵梓萱 | `dev-stu-09@dev.campuslink.local` | 模拟学生09 |
| 10 | `888800010` | 赵宇轩 | `dev-stu-10@dev.campuslink.local` | 模拟学生10 |
| 11 | `888800011` | 赵语汐 | `dev-stu-11@dev.campuslink.local` | 模拟学生11 |
| 12 | `888800012` | 赵泽楷 | `dev-stu-12@dev.campuslink.local` | 模拟学生12 |
| 13 | `888800013` | 赵静姝 | `dev-stu-13@dev.campuslink.local` | 模拟学生13 |
| 14 | `888800014` | 赵亦辰 | `dev-stu-14@dev.campuslink.local` | 模拟学生14 |
| 15 | `888800015` | 赵婉清 | `dev-stu-15@dev.campuslink.local` | 模拟学生15 |
| 16 | `888800016` | 赵博文 | `dev-stu-16@dev.campuslink.local` | 模拟学生16 |
| 17 | `888800017` | 赵念安 | `dev-stu-17@dev.campuslink.local` | 模拟学生17 |
| 18 | `888800018` | 赵景行 | `dev-stu-18@dev.campuslink.local` | 模拟学生18 |
| 19 | `888800019` | 赵清和 | `dev-stu-19@dev.campuslink.local` | 模拟学生19 |
| 20 | `888800020` | 赵知夏 | `dev-stu-20@dev.campuslink.local` | 模拟学生20 |
| 21 | `888800021` | 钱子涵 | `dev-stu-21@dev.campuslink.local` | 模拟学生21 |
| 22 | `888800022` | 钱雨桐 | `dev-stu-22@dev.campuslink.local` | 模拟学生22 |
| 23 | `888800023` | 钱浩然 | `dev-stu-23@dev.campuslink.local` | 模拟学生23 |
| 24 | `888800024` | 钱思远 | `dev-stu-24@dev.campuslink.local` | 模拟学生24 |
| 25 | `888800025` | 钱嘉懿 | `dev-stu-25@dev.campuslink.local` | 模拟学生25 |
| 26 | `888800026` | 钱若曦 | `dev-stu-26@dev.campuslink.local` | 模拟学生26 |
| 27 | `888800027` | 钱沐辰 | `dev-stu-27@dev.campuslink.local` | 模拟学生27 |
| 28 | `888800028` | 钱书瑶 | `dev-stu-28@dev.campuslink.local` | 模拟学生28 |
| 29 | `888800029` | 钱梓萱 | `dev-stu-29@dev.campuslink.local` | 模拟学生29 |
| 30 | `888800030` | 钱宇轩 | `dev-stu-30@dev.campuslink.local` | 模拟学生30 |
| 31 | `888800031` | 钱语汐 | `dev-stu-31@dev.campuslink.local` | 模拟学生31 |
| 32 | `888800032` | 钱泽楷 | `dev-stu-32@dev.campuslink.local` | 模拟学生32 |
| 33 | `888800033` | 钱静姝 | `dev-stu-33@dev.campuslink.local` | 模拟学生33 |
| 34 | `888800034` | 钱亦辰 | `dev-stu-34@dev.campuslink.local` | 模拟学生34 |
| 35 | `888800035` | 钱婉清 | `dev-stu-35@dev.campuslink.local` | 模拟学生35 |
| 36 | `888800036` | 钱博文 | `dev-stu-36@dev.campuslink.local` | 模拟学生36 |
| 37 | `888800037` | 钱念安 | `dev-stu-37@dev.campuslink.local` | 模拟学生37 |
| 38 | `888800038` | 钱景行 | `dev-stu-38@dev.campuslink.local` | 模拟学生38 |
| 39 | `888800039` | 钱清和 | `dev-stu-39@dev.campuslink.local` | 模拟学生39 |
| 40 | `888800040` | 钱知夏 | `dev-stu-40@dev.campuslink.local` | 模拟学生40 |
| 41 | `888800041` | 孙子涵 | `dev-stu-41@dev.campuslink.local` | 模拟学生41 |
| 42 | `888800042` | 孙雨桐 | `dev-stu-42@dev.campuslink.local` | 模拟学生42 |
| 43 | `888800043` | 孙浩然 | `dev-stu-43@dev.campuslink.local` | 模拟学生43 |
| 44 | `888800044` | 孙思远 | `dev-stu-44@dev.campuslink.local` | 模拟学生44 |
| 45 | `888800045` | 孙嘉懿 | `dev-stu-45@dev.campuslink.local` | 模拟学生45 |

**旁路名册里的 4 个内置账号仍在**（`app.roster.bypass=true` 时生效，学号须 9 位数字）：`249971346/张三`、`249971347/李四`、`249971348/王五`、`249971349/赵六`——它们**只能用于注册**（走内置名册），与本批 45 个互不相干。

## 4. 怎么重建（清库后 / 换机器时）

脚本默认 **dry-run**，只读不写；加 `--apply` 才真发请求。

```bash
# 前置：本机 MySQL(campuslink 库) + Redis 已起，后端已启动，且 venv 已就绪（backend/scripts/.venv）
cd backend/scripts

# 干跑：只看会做什么 + 当前库里有多少
PYTHONIOENCODING=utf-8 ./.venv/Scripts/python.exe data/D001__seed_dev_roster_45.py

# 真跑：名册导入 + 45 个账号注册 + 抽 3 个账号登录复核
# ⚠️ 必须指向一个 APP_ROSTER_BYPASS=false 的实例，否则学籍核验走内置名册、这 45 个学号一律 2101
APP_ROSTER_BYPASS=false VERIFY_IP_HOURLY_LIMIT=60 SERVER_PORT=8089 mvn -o spring-boot:run   # 另起一个实例（backend/ 下）
API_BASE=http://localhost:8089 PYTHONIOENCODING=utf-8 ./.venv/Scripts/python.exe data/D001__seed_dev_roster_45.py --apply
```

**为什么必须 bypass=false**：`campuslink.roster.bypass=true`（开发默认）会把 `InMemoryTestRosterGateway` 顶上来、**遮蔽数据库名册**，此时数据库里那 45 行对学籍核验完全不可见。**登录不受影响**（登录只查 `users`，不碰名册），所以日常联调用默认配置即可，只有**重建这批账号**时才需要临时关旁路。

**幂等**：名册侧服务端 `saveIfAbsent`，重跑得到 `inserted=0 skipped=45`；注册侧学号已占用 ⇒ 学籍核验返回 `2101` 被跳过，**不会产生重复账号**（2026-09-15 已实测两次连跑）。

其他可用开关：`DB_HOST` / `DB_PORT` / `DB_NAME` / `DB_USER` / `DB_PASSWORD`、`ADMIN_EMAIL`、`CAPTCHA_CODE`、`LOGIN_CHECK`（登录复核个数，默认 3）、`CAPTCHA_RESEND_WAIT`（遇 `2002` 的等待秒数，默认 61）、`--stage {roster,register,all}`。

## 5. 怎么清理

```sql
-- 先删账号（外键无约束，但业务上先删用户更清晰）
DELETE FROM users WHERE nickname LIKE '模拟学生%';
DELETE FROM student_roster WHERE source_batch = 'dev-sim-45';
```

⚠️ **不可逆的一点**：若这批账号已经发过帖 / 回过帖，删 `users` 会让 `posts.user_id` 悬空（无外键约束，不会报错，但列表页昵称会变空）。**清理前先确认这批账号有没有产生内容**；有内容就不要删账号，只删名册行。

## 6. 边界与红线

- 本清单与这批数据**只对本机开发库有意义**：`campuslink.roster.bypass` 生产必须为 `false`、`campuslink.captcha.fixed-code` 生产必须**留空**——两者都是 [AGENTS.md](../../AGENTS.md)「红线」小节点名的**上线检查清单项**，发布阶段的落地载体是[发布检查清单模板](../模板/发布检查清单模板.md)；
- 名册导入**写了审计日志**（`audit_logs` 表 `action=ROSTER_IMPORT`，含批次与 inserted/skipped/failed 计数），满足"名册导入必须写审计日志"的生产红线；
- 学号在库里仍是 `student_id_enc`（AES-GCM）+ `student_id_hash`（HMAC），**本清单的明文只存在于这个文档和脚本里**，属"合成身份的开发期便利"，不改变任何接口的返回口径（任何接口仍不得返回 `*_enc` / `*_hash`）；
- **B1 / B2 真实名册到位后**：这批模拟数据应按 §5 清掉，再做一次关闭 bypass 的全链路回归（届时 `RosterGatewayDbImpl` 才是首次被真实数据验证）。

## 7. 关联

- 实施方案与验证记录：[CR-054-学籍名册模拟数据](实施方案/CR-054-学籍名册模拟数据.md)
- 变更登记：[变更台账 §1 CR-054](../变更日志/变更台账.md#cr-054)
- 数据脚本规范：[backend/scripts/README.md](../../backend/scripts/README.md)
- 阻塞项口径：[下一步行动清单](../下一步行动.md)（B1 / B2 仍未到位）
