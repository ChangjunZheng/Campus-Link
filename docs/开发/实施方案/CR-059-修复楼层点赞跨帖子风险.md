# CR-059 · 修复楼层点赞跨帖子操作风险

| 文档信息 | 内容 |
|---|---|
| 版本 | v1.0 |
| 状态 | 已实施 |
| 维护人 | AI 会话 |
| 最后更新 | 2026-09-17 |
| 关联问题 | [项目现有问题清单](../../项目现有问题清单.md#bug-001楼层点赞存在跨帖子操作风险) |

## 1. 目标

修复楼层点赞接口忽略路径 `postId` 的问题，确保只有当 `replyId` 确实属于路径指定的可见帖子时，才允许执行点赞 / 取消点赞。

## 2. 范围

- **做**：
  - `ReplyController` 将 `postId` 传入互动 application 服务；
  - application 层校验父帖存在且可见、回复存在且归属该父帖；
  - 跨帖子请求统一返回现有 `NOT_FOUND / 3001` 语义；
  - 补 controller 透传测试和 application 跨帖子拒绝测试。
- **不做**：
  - 不处理 `BUG-002` 的 `Reply.status` 过滤问题；
  - 不新增接口路径、请求参数或响应字段；
  - 不修改数据库、Flyway 迁移、OpenAPI 快照；
  - 不改变正常点赞、取消点赞、通知和计数逻辑。

## 3. 改动点（文件级）

| 文件 | 动作 | 说明 |
|---|---|---|
| `backend/src/main/java/com/campuslink/module/forum/web/ReplyController.java` | 改 | 调用 `toggleReplyLike(userId, postId, replyId)`，保留现有鉴权与响应形状 |
| `backend/src/main/java/com/campuslink/module/forum/application/InteractionApplicationService.java` | 改 | 增加 `postId` 入参；先校验可见父帖，再校验回复的 `postId` 与路径一致 |
| `backend/src/test/java/com/campuslink/module/forum/web/InteractionControllerAuthTest.java` | 改 | 更新方法签名断言，确认 `postId` 被透传 |
| `backend/src/test/java/com/campuslink/module/forum/application/InteractionApplicationServiceTest.java` | 改 | 增加跨帖子回复拒绝、父帖不可见拒绝、正常同帖点赞回归 |
| `docs/项目现有问题清单.md` | 实施后更新 | 将 BUG-001 标记为已修复，记录 CR-059、测试结果和 BUG-002 保留事项 |

## 4. 步骤

- [x] 1. 修改 application / web 方法签名并加入父帖与回复归属校验。
- [x] 2. 补充跨帖子、不可见父帖和正常同帖点赞测试。
- [x] 3. 执行 `mvn verify`，确认架构守护与论坛互动回归全部通过。
- [x] 4. 更新问题清单；确认 OpenAPI 快照无变化。

## 5. 验证

- **单测**：
  - 同帖 `postId=9, replyId=11`：继续正常 toggle；
  - 跨帖 `postId=9, replyId=11` 且回复属于帖子 8：返回 `NOT_FOUND`，不调用点赞仓储、计数更新或通知；
  - 父帖不存在 / 已下架：返回 `NOT_FOUND`，不触发回复点赞。
- **契约**：接口路径、请求参数和响应结构不变，`openapi.json` 预期无 diff。
- **回归面**：帖子点赞、收藏、通知、自触发抑制和匿名 401 行为不变。

## 6. 回滚

仅涉及 Java 方法签名、校验逻辑与测试；如需回滚，恢复本 CR 的代码和测试改动即可，无数据库回补。

## 7. 实施结果（实施后追加，不回改上文）

2026-09-17 已实施：

- `ReplyController` 将 `postId` 透传给 `InteractionApplicationService`；
- application 层先校验父帖可见，再校验回复归属父帖；跨帖子和父帖不可见统一返回 `NOT_FOUND / 3001`；
- 新增跨帖子拒绝、父帖不可见拒绝测试，更新 controller 透传断言；
- `mvn verify`：187/187 全绿，`ArchitectureGuardTest` 11 例全绿；
- 未修改接口路径、请求参数、响应结构、数据库和 OpenAPI 快照；
- `BUG-002`（已下架回复仍可点赞或采纳）按原计划保留，未在本 CR 处理。
