# CR-052 · 前端界面美化（适度品牌化，纯展示层）

| 文档信息 | 内容 |
|---------|------|
| 版本 | v1.0 |
| 状态 | 待确认 → 已确认 → 已实施 |
| 维护人 | AI 会话（前端美化会话） |
| 最后更新 | 2026-09-14 |
| 关联 CR | [CR-052](../../变更日志/变更台账.md#cr-052) |

## 1. 目标

在不推翻 [CR-023](../../变更日志/变更台账.md#cr-023) / [CR-024](../../变更日志/变更台账.md#cr-024) / [CR-025](../../变更日志/变更台账.md#cr-025) 沉淀的令牌体系前提下，按发起人选定的**方案二「适度品牌化」**提升 9 个页面的视觉层次与精致度：首页有品牌氛围、版块可一眼区分、列表结构统一、详情楼层有重点、登录页有品牌感、全局有连贯的交互反馈。**纯展示层：不改任何接口 / 路由 / 数据流 / 后端 / 依赖。**

## 2. 范围

- **做**（六组，分 5 批落地，每批停下汇报）：
  - **A 全局质感**：顶栏 sticky + 滚动后毛玻璃底；路由切换淡入；交互过渡时长令牌；`:focus-visible` 键盘焦点环；细滚动条；主区间距与页脚细化；
  - **B 首页**：hero 重排为品牌横幅（左 slogan/按钮，右「快速开始」面板）；6 版块卡配线性图标 + 识别色淡底图标容器 + hover 微抬；
  - **C 列表统一**：新增 `components/PostListItem.vue`，首页 / 版块 / 搜索 / 收藏四处列表改用它（**闭环 [界面规范](../../设计/UI规范/界面规范.md) §5.1 挂账的 P0**），首页补摘要、行 hover 底色、回复数轻量徽章；
  - **D 详情页**：最佳答案楼层整体浅绿底 + 左侧竖条强调；楼层号徽标；`authorId` 比对显示「楼主」（复用 [CR-043](../../变更日志/变更台账.md#cr-043) 已透出字段，零接口改动）；
  - **E 登录页**：桌面双栏（左品牌区：logo / slogan / 三条价值主张；右现有表单卡），窄屏折叠为顶部品牌条，`meta.bare` 与两步注册流程不变；
  - **F 发帖页**：Markdown 工具条由裸文字（B/I）改线性图标按钮。
- **不做**：
  - 不加任何 npm 依赖（**`highlight.js` 代码高亮是 ADR-005 功能欠账，不混入本 CR**，要做另开）；不换组件库、不引图表 / 动效库；
  - 不改令牌既有取值（主色 / 链接色 / 字号 / 间距 / 圆角**只新增不修改**）；字重仍只用 400 / 500；圆角不加大；无插画、无位图、无卡片投影（浮层阴影维持现状）；
  - 不改后端、不重生成 OpenAPI 快照（`git diff openapi.json` 应为空）；不改四态（骨架 / 空 / 错 / 3001）、分页口径、路由守卫、`?floor=` 定位等既有交互逻辑；
  - 不做深色模式开关、不做左侧栏、不做通知页句式重构（通知列表仅做行 hover 等通用润色）。
- **对 [界面规范](../../设计/UI规范/界面规范.md) §2「装饰为零」的定点偏离（仅 4 处、可枚举，随本 CR 在该规范加注，非推翻）**：
  1. hero **全站唯一**一处极淡主色渐变底（主色 8% → 透明）；2. 6 版块各一低饱和识别色，**仅**用于图标容器底 / 小点缀，不做文字色（守 AA）；3. 版块卡 hover 允许 `translateY(-2px)`，反馈仍靠主色描边 + 底色、**不用投影**；4. 毛玻璃 `backdrop-filter` **仅** sticky 顶栏一处。

## 3. 改动点（文件级）

| 文件 | 动作 | 说明 |
|------|------|------|
| `frontend/src/styles/tokens.css` | 改 | 新增动效 / 焦点环 / hero / 6 版块色 / success-soft 令牌；组件级规则：滚动条、focus-visible、卡片 hover 微抬、最佳答案楼层、全局过渡 |
| `frontend/src/styles/tailwind.css` | 改 | `@theme inline` 补映射新令牌（工具类仍只管自有元素） |
| `frontend/src/constants/boards.ts` | 改 | 每版块加 `icon` 字段（EP 图标组件名，以已装 2.3.1 实际导出为准，缺则换同义图标） |
| `frontend/src/components/PostListItem.vue` | 新增 | 统一帖子列表项（props：`post` / `showBoard` / `showSummary`） |
| `frontend/src/App.vue` | 改 | sticky 顶栏 + 滚动监听状态 class、`<transition>` 路由淡入、页脚细化 |
| `frontend/src/views/HomeView.vue` | 改 | hero 品牌化、版块卡图标化、列表换 `PostListItem`（补摘要） |
| `frontend/src/views/BoardView.vue` | 改 | 版块头加图标淡底、列表换组件 |
| `frontend/src/views/SearchView.vue` · `MyFavoritesView.vue` | 改 | 列表换组件，四态 / 筛选 / 分页逻辑不动 |
| `frontend/src/views/PostDetailView.vue` | 改 | 最佳答案楼层强调、楼层徽标、楼主标 |
| `frontend/src/views/LoginView.vue` | 改 | 品牌双栏，表单逻辑零改动 |
| `frontend/src/views/PublishView.vue` | 改 | 工具条图标化，`wrap()` 逻辑不动 |
| `frontend/src/views/NotificationsView.vue` | 改（轻） | 行 hover / 间距统一 |
| `docs/设计/UI规范/界面规范.md` · `docs/README.md` §2 | 改 | 规范加注 4 处定点偏离 + 版本登记 |
| `docs/变更日志/变更台账.md` · `docs/进度驾驶舱.html` | 改 | CR 登记与收尾聚合 |

## 4. 步骤

- [ ] 1. **批 1 基座**：tokens.css / tailwind.css / App.vue（sticky、淡入、焦点环、滚动条、页脚）→ `npm run build`；
- [ ] 2. **批 2 首页 + 统一组件**：boards.ts、`PostListItem.vue`、HomeView、BoardView → build；
- [ ] 3. **批 3 其余列表 + 详情**：Search / Favorites / Notifications 润色、PostDetail 楼层 → build；
- [ ] 4. **批 4 登录页 + 发帖工具条** → build；
- [ ] 5. **批 5 真机走查 + 文档同步**：起栈逐页走查 9 路由（桌面 1440 / 窄屏 375，争取截图归档）、界面规范加注、README / 台账 / 驾驶舱。

## 5. 验证

- **构建**：每批 `npm run build`（vue-tsc + vite）零错误；无新增依赖；
- **真机**：`npm run dev` 走查 9 路由——hero / 版块卡 / 四类列表 / 最佳答案楼层 / 登录两 tab 与注册两步 / 发帖工具条；窄屏无横向溢出、登录双栏正确折叠、sticky 顶栏折两行机制不回归；
- **契约**：`docs/设计/接口契约/openapi.json` 的 `git diff` **必须为空**（纯前端）；后端单测基线 162 不变；
- **回归面**：① 新增颜色全部消费令牌（grep 无硬编码十六进制色值）；② EP 组件内部仍只由 tokens.css 的 CSS 变量覆写，不用工具类强压（层叠边界）；③ 版块识别色不承担文字信息（AA 不破）；④ 四态 / 分页 / 相对时间 / 路由守卫 / `?floor=` 高亮 / 通知角标轮询行为不变；⑤ 点赞收藏采纳等按钮的显隐逻辑不动。

## 6. 回滚

纯前端展示层 + 文档，无迁移 / 无依赖 / 无数据。整体回滚 = revert 本 CR 单个提交；分批回滚 = 按批还原对应文件（新增的 `PostListItem.vue` 删除后四处视图还原内联结构即可）。不可逆部分：无。

## 7. 实施结果（实施后追加，不回改上文）

（待实施）
