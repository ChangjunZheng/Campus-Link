# CR-055 · 前端界面优化（代码高亮 / 头像 / 图标 / 中文字体 + 桌面走查）

| 文档信息 | 内容 |
|---------|------|
| 版本 | v1.0 |
| 状态 | 已确认（发起人 2026-09-15「开始实施」）→ 实施中 |
| 维护人 | AI 会话（前端界面优化） |
| 最后更新 | 2026-09-15 |
| 关联 CR | [CR-055](../../变更日志/变更台账.md#cr-055) —— 已于 2026-09-15 登记（[台账](../../变更日志/变更台账.md) §1 行 + §2 明细小节含 `#cr-055` 锚点 + §4 v1.51 + 头部版本 + 统计行；[docs/README](../../README.md) §2 同步）。**编号说明**：登记时台账 §4 已记"下一个自由编号为 CR-055"，本条兑现，其后为 **CR-056** |

命中 [AGENTS.md](../../../AGENTS.md)「实施方案约定」两条门槛：**新依赖**（`highlight.js`、字体分片包）+ **跨多视图与令牌层**。

## 1. 目标

把 [界面规范](../../设计/UI规范/界面规范.md) §9.1 **仅剩的 P0 挂账（④ highlight.js，ADR-005 欠账）做掉**，并用本地 / GitHub 免费开源素材补齐三处真实缺口（头像不可见、图标体系混用、中文字体走系统栈），最后补上 [CR-052](CR-052-前端界面美化.md) 遗留的**桌面 ≥768 目视走查**。量化口径：帖子详情页代码块着色生效；顶栏头像可见且**同一昵称永远同一头像**；自有元素图标 100% Tabler；全站中文字体走自托管 webfont 分片；`openapi.json` **零 diff**、后端**零改动**、单测基线 **164 不触碰**。

## 2. 范围

### 做（五批，**逐批停下验收**）

1. **代码高亮（P0 欠账）**：引入 `highlight.js`（BSD-3-Clause），按需注册约 20 种语言；详情页与楼层两处 `v-html` 渲染后对 `pre code` 着色；github 主题色板**抄成新令牌**而非直接引入其 CSS。
2. **头像体系（仅前端侧）**：新增 `UserAvatar.vue`——有 `avatarUrl` 用之，否则按昵称稳定哈希从 **8 个 pastel 底色令牌**中取一个、文字用**昵称真实首字**、字色按底色亮度自动选深/浅以保 ≥4.5:1；接入顶栏 / 列表项 / 详情页；**顺带移除 `App.vue` 的头像内联样式**（改走新增 `--el-avatar-*` 令牌）。
   > **口径变更（2026-09-15，发起人选定）**：原设想是"本地 8 个占位 SVG 入仓、按哈希取其一"。核实素材后推翻——`04-头像占位/` 的 8 个 SVG 是 **DiceBear「Initials」风格、CC0 1.0**（**许可无问题，原 ⚠️ 解除**），但自带**白字 + 浅色底**，实测对比度仅 **1.27~2.03:1**（连大字 3:1 门槛都不到，首字母实际不可读），且字母由 DiceBear 按种子生成、**与用户名无关**（`zhangsan.svg` 显示 "DF"、`lisi.svg` 显示 "CC"）。故改为**只取其 8 个底色抄成 `--cl-avatar-*` 令牌、头像本体由代码派生**：不入仓任何 SVG 文件、首字母与本人一致、颜色全走令牌。
3. **图标统一为 Tabler**（MIT）：本地 SVG 入仓 + `SvgIcon.vue`（`import.meta.glob` 内联，**零新构建依赖**），替换 17 个自有调用点与 `constants/boards.ts` 的 6 个版块图标。
   > **口径变更（2026-09-15，实施中收窄）**：原设想"40 个本地 SVG 全量入仓"。两点实测推翻——① `import.meta.glob(..., { eager: true })` 是**编译期内联**，未被任何调用点引用的图标会白进首屏包（43 个全量实测 16092 B，实际只用得到 18 个、6446 B），故**只入仓实际引用的 18 个**，素材目录仍保留全量供后续取用；② 本地素材**缺 3 个必需图标**（发布页工具条的 `list` / `photo`、最佳答案标识的 `circle-check`），已按发起人"可拉取 GitHub 开源素材"的授权从 Tabler 上游仓库拉取，**MIT 许可证全文一并入仓**（`src/assets/icons/LICENSE`——本项目对素材做了归一化改写，MIT 要求保留版权与许可声明）。
   > **另一处实测发现**：图标容器**不能复用 `el-icon`**——EP 的 `.el-icon` 带 `fill: currentColor`，CSS 优先级高于 SVG 自身的 `fill="none"` presentation attribute，会把 Tabler 描边图标**灌成实心块**。故 `SvgIcon.vue` 自带 `.cl-icon` 容器。
4. **中文字体子集化自托管**：用**中文网字计划 `cn-fontsource`** 的 woff2 分片包（思源黑体 SC，SIL OFL 1.1），`unicode-range` 按需加载，只取 Regular(400) + Medium(500) 两档。**实施前须先核实两件事**（不过则砍掉本批）：① npm 包内**是否含 woff2 实体文件**（若 `@font-face` 指向其 CDN 则不满足自托管）；② 分片包许可证是否允许再分发。
   > **口径变更（2026-09-15，实施前核实后换包）**：原定的两道门**两个候选包都过了**——`cn-fontsource-source-han-sans-sc-vf@1.0.10` 与 `@fontsource-variable/noto-sans-sc@5.3.0` 均含 **woff2 实体文件 + 相对 `url()`**（CSS 内 `grep https?://` 零命中，不指向任何 CDN）、均为 **SIL OFL 1.1**（允许再分发与网页嵌入，须随附许可声明）。但逐项实测后 cn-fontsource 有四处硬伤，故**改用 `@fontsource-variable/noto-sans-sc`**：
   >
   > | 核实项 | cn-fontsource-source-han-sans-sc-vf@1.0.10 | @fontsource-variable/noto-sans-sc@5.3.0 |
   > |---|---|---|
   > | `font-weight` 声明 | **0 处**——缺该描述符即只有 400 一档，全站 `font-medium`(500) 无匹配字面，会退化成浏览器伪粗 | `100 900` 可变轴，**400 / 500 都是真字形** |
   > | 覆盖本项目 772 个静态码点需下载 | 43 片 / **2192.9 KB** | 23 片 / **1206.2 KB** |
   > | `font-display` | L1 用 `block`、其余 `swap`（混用；`block` 会阻塞首屏文字渲染） | 全部 `swap` |
   > | 码点覆盖 | 770 / 772（**缺 2 字**） | 772 / 772 |
   > | 包体 / 文件数 | 17.21 MB / 1270 | 4.31 MB / 101 |
   > | 维护 | 2023-02 停更 | `lastModified` 2026-01-07，字体版本 v40 |
   >
   > 两者是同一字型（Noto Sans SC = 思源黑体 SC 的 Google 发行名），观感一致，故换包不改变本条的设计意图。
   > **⚠️ 供发起人行使 §4-5 目视否决权的成本事实**（实测，非估算）：即便选更轻的那个，全站静态 UI 文案也要拉 **23 个分片 / 1206.2 KB**（平均每片 52.4 KB），帖子正文是 UGC、含字更广会继续追加分片；`dist` 因 Vite 会把 CSS 引用到的 woff2 全量产出而**增加 4.31 MB**（浏览器只按 `unicode-range` 下命中的那部分，但部署产物变大）。而被替换掉的系统栈在 Windows 是微软雅黑、macOS/iOS 是苹方，**两者本身都是高质量中文字面、下载 0 字节**。这是本批"观感收益 vs 体积成本"的核心权衡。
5. **桌面 ≥768 全站走查 + 令牌扫尾**：9 真页 + 2 占位页逐页目视（**移动端一侧按 [CR-053](../../变更日志/变更台账.md#cr-053) 暂缓，本轮不做**），对照 [PRD](../../需求/产品需求文档.md) §5.1 确认无断链；复测全站无字面 hex / 无 `text-[Npx]` / 字重仅 400·500。**可选（请发起人点头才做）**：重摄 `docs/设计/UI规范/screenshots/` 的桌面 6 张（现档已双重过时：色值随 [CR-024](../../变更日志/变更台账.md#cr-024) 过时、布局随 [CR-025](../../变更日志/变更台账.md#cr-025) 过时）。

### 不做（每条给理由，防边界漂移）

| 不做 | 理由 |
|---|---|
| **深色模式** | [界面规范](../../设计/UI规范/界面规范.md) §9.2 与 [CR-052](CR-052-前端界面美化.md) 双重排除；发起人本轮亦明确不纳入 |
| **个人主页 `/u/:id`** | [CR-036](../../变更日志/变更台账.md#cr-036) 增量 PRD **待评审，评审通过前不得开工**；保留 `Placeholder.vue` |
| **管理后台 `/admin`** | [技术方案](../../设计/技术方案.md) §5 的 `/api/v1/admin/...` 仍是计划项、**无端点**；保留 `Placeholder.vue` |
| **论坛 VO 补 `authorAvatarUrl`** | 实测 `forum/web/vo/` 5 个 VO **均不带 avatar** → 帖子列表 / 详情 / 楼层拿不到真实头像 URL。补它 = 接口契约变更 + 快照重生成 + 后端改动，**另开 CR**；本轮头像只能是前端派生的占位 |
| **头像上传 / 设置** | 无接口（`users.avatar_url` 存在但注册链路从不写入，恒 null）；属 [PRD](../../需求/产品需求文档.md) F-ACC-002 后端能力 |
| **空状态插画**（unDraw / Open Peeps 等） | 违 §5.2「插画为零」与 [CR-052](CR-052-前端界面美化.md) 的**封闭 4 处放宽清单**（新增即违例，须另开 CR） |
| **EP 组件内部图标改造** | `el-select` 箭头 / `el-input` 清除 / `el-date-picker` 等**无法替换**（须 fork EP 组件）→ 图标只能做到**混用体系**，见 §3 限制 |
| **`unplugin-icons` / `@iconify/json` / `vite-plugin-svg-icons` / DiceBear** | `import.meta.glob` + 本地 40 个 SVG 已零依赖达成；不为此再增构建期或运行时依赖 |
| **`react-bits` 代码复用** | React 库，Vue 3 **不可复用**；只作视觉参考，不入依赖、不入仓 |
| **32MB OTF 全量自托管** | 思源黑体 SC 两个 OTF 各 16MB，全量入仓不可接受；OTF 只留素材目录 |
| **`pyftsubset` 自制常用字子集** | 帖子正文是 UGC，**含生僻字即掉字回退**，混排比不换字体更差；且引入 Python 构建期工具链 |
| **HarmonyOS Sans SC / MiSans** | 许可条款含"不得修改字体本身再分发"等限制，且本项目已持有 OFL 的思源黑体，无理由换 |
| **代码块复制按钮 / 行号 / 折叠** | 新增交互而非还原度问题，EP 无对应组件需自研 |
| **移动端 / 窄屏走查与重摄** | [CR-053](../../变更日志/变更台账.md#cr-053) 已按发起人指示暂缓；已落地窄屏代码**不回退** |
| **后端任何一行、`openapi.json`、Flyway 迁移** | 本轮是纯前端展示层，与 [CR-052](CR-052-前端界面美化.md) 同边界 |

## 3. 改动点（文件级）

| 文件 | 动作 | 说明 |
|------|------|------|
| `frontend/package.json` | 改 | 新增 `highlight.js`；批次 4 核实后**只新增 1 个包** `@fontsource-variable/noto-sans-sc`（原设想 `cn-fontsource-*` 1~2 个，换包理由见 §2 第 4 条口径变更） |
| `frontend/src/utils/highlight.ts` | 新增 | `highlight.js/lib/core` + 按需注册约 20 语言（java / javascript / typescript / python / sql / bash / json / xml / css / go / c / cpp / csharp / php / ruby / rust / kotlin / swift / yaml / markdown / diff / plaintext）；**不用 `lib/common`**（37 语言，体积近翻倍） |
| `frontend/src/views/PostDetailView.vue` | 改 | 两处渲染点（正文 `v-html` 约 :260、楼层约 :375）后 `watch` + `nextTick` 调 `hljs.highlightElement`；接 `UserAvatar` |
| `frontend/src/styles/hljs.css` | 新增 | 约 30 行，把 `.hljs-*` 类映射到新增 `--cl-code-*` 令牌 |
| `frontend/src/styles/tokens.css` | **只新增** | `--cl-code-*` 色板（取自本地 `03-代码高亮主题/github.min.css`，BSD-3）+ `--cl-avatar-1~8` 底色（取自本地 `04-头像占位/` 的 DiceBear CC0 pastel）+ `--el-avatar-bg-color` / `-text-color` / `-border-radius` + `--cl-font-family` 前置 webfont（既有系统栈保留为回退）。**既有令牌值一个不改** |
| `frontend/src/main.ts` | 改 | import `hljs.css`；批次 4 import 字体分片 CSS |
| `frontend/src/components/UserAvatar.vue` | 新增 | props `src?` / `name` / `size`；`src` 有值→用之，否则按 `name` 稳定哈希取 8 个底色令牌之一 + 昵称首字，`name` 亦空→回退 EP 默认底色 |
| `frontend/src/assets/avatars/*.svg` | ~~新增（8 个）~~ **不入仓** | **口径变更（2026-09-15，发起人选定）**：本地 `04-头像占位/` 已核实为 **DiceBear「Initials」· CC0 1.0**（原 ⚠️「来源与许可未登记」**解除**），但其自带白字浅底对比度仅 **1.27~2.03:1**、字母与用户名无关，故**只把 8 个底色抄成 `--cl-avatar-*` 令牌**，头像本体由 `UserAvatar.vue` 派生，不复制任何 SVG 入仓 |
| `frontend/src/components/SvgIcon.vue` · `src/assets/icons/index.ts` | 新增 | `import.meta.glob('./*.svg', { query: '?raw', import: 'default', eager: true })`；Tabler 用 `stroke="currentColor"`，颜色走令牌。**不复用 `el-icon`**（EP 的 `fill: currentColor` 会把描边图标灌成实心块），自带 `.cl-icon` 容器；`IconName` 用**显式字面量联合类型**——Vite 的 glob 类型是 `Record<string, T>`、键非字面量，推不出名册，不显式声明则图标名写错只会静默渲染空白 |
| `frontend/src/assets/icons/*.svg` | 新增（**18 个，实测 6446 B**） | 15 个取自本地 `01-图标库-tabler/`、3 个（`list` / `photo` / `circle-check`）本地缺、从 Tabler 上游拉取；**只入仓实际被引用的**（eager glob 会把未用图标一并内联进首屏包；43 个全量实测 16092 B，裁剪省下 9646 B ≈ 60%），素材目录仍保留全量 43 个 |
| `frontend/src/assets/icons/LICENSE` | 新增 | Tabler Icons MIT 许可全文（Copyright (c) 2020-2026 Paweł Kuna，1073 B）——入仓副本做过归一化改写（去上游注释头与 `icon-tabler-*` 类名、压成单行），MIT 要求随附版权声明 |
| `frontend/src/constants/boards.ts` | 改 | 图标由"EP 组件引用"改"图标名串"，**4 个消费点同步** |
| `App.vue` · `PostListItem.vue` · `BoardView` · `LoginView` · `HomeView` · `PublishView` · `SearchView` · `Placeholder.vue` | 改 | 17 个 EP 图标调用点换 `SvgIcon`；`App.vue` 另移除头像内联样式 |
| `docs/设计/UI规范/界面规范.md` | 加注 | §9.1 P0④ 置 ✅、§5.6 高亮状态回填、新增「字体」与「头像 / 图标」约定小节；**原文保留只加注** |
| `docs/变更日志/变更台账.md` · `docs/README.md` §2 · `docs/进度驾驶舱.html` | 登记 | **待方案确认后**与实施同批做（本轮不动） |

**两处如实限制**：① 批次 3 后图标是**混用体系**——自有元素 100% Tabler，**EP 组件内部图标仍是 EP 自带**（可及的少数例外：`el-pagination` 的 `:prev-icon` / `:next-icon` props）；② 批次 3 会**推翻 [CR-052](CR-052-前端界面美化.md) 刚做的"发布页工具栏图标化"选型**（其注明"取自 `@element-plus/icons-vue`，零新依赖"）。

## 4. 步骤

- [ ] 1. 素材落地：Tabler 40 SVG / 头像 8 SVG 拷入 `frontend/src/assets/`；确认 `04-头像占位/` 来源与许可（**未确认则批次 2 只做 `avatarUrl` 渲染、不做占位图**）
- [ ] 2. **批次 1（代码高亮）**：装 `highlight.js` → `utils/highlight.ts` → `tokens.css` 新增 `--cl-code-*` → `styles/hljs.css` → 详情页两处接入 → `npm run build` → **停下验收**
- [ ] 3. **批次 2（头像）**：`UserAvatar.vue` → `--el-avatar-*` 令牌 + 移除内联样式 → 接入顶栏 / 列表 / 详情 → build → **停下验收**
- [ ] 4. **批次 3（图标）**：`SvgIcon.vue` + `assets/icons/index.ts` → 17 调用点 → `boards.ts` 与 4 消费点 → build → **停下验收**
- [ ] 5. **批次 4（字体）**：先核实分片包**含 woff2 实体** + 许可证 → 装包 + `main.ts` import → `--cl-font-family` 前置 → build + 记 `dist` 体积 → **停下由发起人目视确认全站观感**（观感不过即按 §6 整批回退）
- [ ] 6. **批次 5（走查 + 扫尾）**：起真栈（MySQL / Redis / 8088 / 5173）→ 桌面 ≥768 逐页走查 9 真页 + 2 占位页 → 令牌三项复测 →（可选）重摄桌面 6 张 → **停下验收**
- [ ] 7. 文档同步：界面规范加注 + 台账 §1 行 / §2 明细 / §4 / 头部版本 / 统计行 + `docs/README.md` §2 + 驾驶舱

## 5. 验证

- **单测**：纯前端，**后端零改动 → `mvn verify` 不在本轮验证面**，基线 **164 保持不变**（收尾仍核一次"未受影响"）。前端无测试框架，验证靠构建 + 真机目视。
- **构建**：每批 `npm run build` 必须通过；批次 3 / 4 后记录 `dist` 体积变化（预期：批次 3 约 +30~40KB JS；**批次 4 显著增大**——全字集分片总和，单字重估 3~5MB，两档翻倍；**构建产物变大但首屏只下载命中的分片**，须如实记进 §7）。
- **真机**（桌面 ≥768）：发一篇含 java / python / sql / json / bash 代码块的帖子 → 目视确认着色生效，并**对比 `contentHtml` 原文确认前端没有二次渲染 Markdown**（ADR-005 红线）；顶栏头像可见且刷新后不变；6 版块图标 + 17 处自有图标显示正常、颜色跟随令牌；全站中文字体观感由发起人判定。
- **契约**：`docs/设计/接口契约/openapi.json` 的 `git diff` **必须为空**（后端零改动）；非空即说明本轮越界，按 §6 回退。
- **令牌自查**：grep 全站（`tokens.css` / `tailwind.css` 之外）**无字面 hex 与 rgba**、**无 `text-[Npx]` / `font-size: Npx`**、**无 400·500 之外的 `font-weight`**（CR-025 曾实测为 0，本轮复测防回归）。
- **回归面**：两处 `v-html` 渲染点；`PostListItem.vue` 的 4 个消费点（首页 / 版块 / 搜索 / 收藏）；`boards.ts` 的 4 个消费点；路由淡入 `transition`；**EP 组件层叠边界**（Tailwind 输出在 `@layer` 内、EP 无层 → 工具类压不过 EP，故 EP 一律走 CSS 变量）；窄屏既有代码不回退。
- **治理边界（如实声明）**：本轮**不闭环任何治理欠账**。桌面走查只推进 [W-03](../../流程偏离记录.md) 补偿③ 的**桌面一侧**，因 [CR-053](../../变更日志/变更台账.md#cr-053) 已暂缓移动端一侧，**补偿③ 整体仍不得置为"已执行"、A3-6 仍开放**；A3-8、B1/B2、W-02 / W-05 / W-07（**不代签**）状态一律不动。

## 6. 回滚

**每批独立可退，无不可逆部分**（纯前端资产：无数据、无迁移、无接口、无密钥）。

- 批次 1：删 `utils/highlight.ts` + `styles/hljs.css`，`main.ts` 与详情页取消引用，`tokens.css` 移除 `--cl-code-*`，`package.json` 卸载 `highlight.js`。
- 批次 2：删 `components/UserAvatar.vue` + `src/assets/avatars/`，`App.vue` 恢复原内联样式，移除 `--el-avatar-*`。
- 批次 3：删 `SvgIcon.vue` + `src/assets/icons/`，17 个调用点与 `boards.ts` 用 `git checkout --` 恢复（**前提：批次 3 单独成 commit**，故实施时每批一提交，便于按批 revert）。
- 批次 4（**风险最高，观感是全站性的**）：`package.json` 移除分片包 + `main.ts` 取消 import + `--cl-font-family` 回退到既有系统栈；`dist` 体积随之回落。
- 素材目录 `D:\Workspace\TechResources\前端素材` 的新增文件**不回滚**（仓库外，属素材留存）。
- 实施后**不回改本方案**：实际与计划的偏差按 [说明与模板](说明与模板.md) 追加到 §7。

## 7. 实施结果（实施后追加，不回改上文）

<待实施>
