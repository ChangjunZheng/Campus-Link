# Campus-Link 项目现有问题清单

| 文档信息 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 快照日期 | 2026-09-17 |
| 适用范围 | 当前仓库 `main` 分支代码、前端、测试、配置与项目文档 |
| 维护性质 | 问题盘点，不替代 PRD、技术方案、变更台账或流程偏离记录 |

## 1. 结论先行

项目当前已经具备账号注册登录、固定版块、发帖、回帖、采纳、点赞收藏、搜索、通知中心，以及热榜后端能力，且当前静态构建验证通过：

- 后端 `mvn verify`：187 个测试全部通过；
- 前端 `npm run build`：构建通过；
- 当前 HEAD：`286739b`；
- 当前仍未达到完整产品交付条件，主要原因不是编译失败，而是 P0 功能缺失、内容安全与运营后台缺失、真实名册链路未验证，以及集成测试和提测门禁不完整。

本次审计确认的最高优先级问题是：

1. 回复点赞接口忽略路径中的 `postId`，且回复写操作没有统一过滤 `status`，存在跨帖子操作和操作已下架回复的风险；
2. P0 的内容机审、举报、内容处置、管理员后台、账号注销、帖子删除、个人主页等功能尚未完成；
3. 首页仍只有“最新”列表，热榜目前只有后端 `sort=hot`，PRD 要求的“最新 / 热门”前端切换未交付；
4. 默认配置仍启用固定验证码、学籍旁路和开发密钥，缺少启动级的生产环境 fail-fast 防护；
5. 现有测试主要是纯单测，没有完整 Spring 上下文、数据库、Redis 和浏览器回归测试，`mvn verify` 不能代表真实启动和联调通过。

## 2. 已确认的未完成功能

### 2.1 P0 功能缺口

| 编号 | 功能 | 当前状态 | 影响 | 证据 |
|---|---|---|---|---|
| F-001 | 真实学籍名册与正式注册链路 | 真实名册仍未到位，开发依赖 `APP_ROSTER_BYPASS=true` 或合成数据；尚未完成关闭 bypass 后的完整回归 | 注册身份可信性仍未被真实数据验证；B1 / B2 未闭环 | `docs/下一步行动.md`、`docs/开发/Sprint1计划.md`、`backend/src/main/resources/application.yml:87-89` |
| F-011 | 手机号注册 / 登录 | PRD F-ACC-001 定义“邮箱或手机号 + 验证码”，当前 Command、Controller、前端表单和发送逻辑均只支持邮箱；数据库虽预留 `phone_enc` / `phone_hash`，但没有业务链路 | P0 需求只完成邮箱分支，不能声称注册登录完整交付 | PRD F-ACC-001；`AccountCommands.java`；`AccountController.java`；`LoginView.vue` |
| F-002 | 账号注销 | 无注销接口、应用服务、前端入口和冷静期任务 | PRD F-ACC-003 为 P0，但用户无法行使注销和撤销权利 | PRD F-ACC-003；当前 `account/web` 无对应映射 |
| F-003 | 个人主页 | `/u/:id` 仍指向 `Placeholder.vue`；后端只有 `/users/me`，没有他人主页、发帖时间线、获赞数、被采纳数等接口 | PRD F-ACC-002 未完成；个人身份展示链路断开 | `frontend/src/router/index.ts:13`、PRD F-ACC-002 |
| F-004 | 帖子删除 | 没有删除帖子接口、应用服务和前端操作；数据库虽有 `status` / `is_deleted`，但没有用户删除用例 | PRD F-FORUM-006 未完成，内容治理也缺少用户自助入口 | 当前 `PostController` 无 `@DeleteMapping`；PRD F-FORUM-006 |
| F-005 | 内容机审 | 发帖与回帖直接渲染并保存，没有第三方审核 gateway、审核结果处理或 fail-closed 开关 | PRD F-SAFE-001 是 P0；前端“发布前将进行内容安全机审”属于未兑现承诺 | `PostApplicationService.java:38-43`、`ReplyApplicationService.java`；`PublishView.vue` |
| F-006 | 举报、内容处置和运营后台 | `/admin` 仍是占位页；后端只有学籍名册导入，没有举报、工单、内容下架、用户禁言 / 封禁等业务接口 | PRD F-SAFE-002 / F-SAFE-003 / F-ADMIN-001~003 全部未完成；上线后没有基本运营闭环 | `frontend/src/router/index.ts:16`；当前 `module` 下无 report / moderation / admin content controller |
| F-007 | 标签闭环 | 数据库有 `tags`、`post_tags` 和 `posts.tags`，但发帖 Command、前端表单和持久化链路都没有标签入参；搜索只能对空的 `posts.tags` 做 LIKE 兜底 | “按标签搜索”实际上没有真实数据来源；PRD F-FORUM-002 / F-FORUM-008 只完成标题搜索 | `PublishPostCommand.java` 无 tags 字段；`frontend/src/api/forum.ts:85-87`；`PostMapper.java` |
| F-012 | 发帖实时预览 | 发帖页右侧明确显示“暂无实时预览”，前端没有 Markdown 预览状态或预览组件 | PRD F-FORUM-002 的实时预览验收项未完成；用户只能发布后查看结果 | `frontend/src/views/PublishView.vue`；PRD F-FORUM-002 |
| F-008 | 首页“最新 / 热门”切换 | 后端已支持 `GET /api/v1/posts?sort=hot`，首页固定调用默认最新排序，没有 tab / segmented control，也没有把 `sort` 传入 API | PRD F-FORUM-003 只交付后端一半，用户无法从首页使用热榜 | `HomeView.vue:19-40`、`frontend/src/api/forum.ts:71-78` |
| F-009 | 回复引用 | `quoted_reply_id` 虽在 V1 表中预留，但回帖 Command、UI 和业务服务均未实现；通知类型 `quote` 也未实现 | PRD F-FORUM-004 / F-SOC-001 的引用回复场景缺失 | `PublishReplyCommand.java`、`NotificationType.java`、`docs/设计/技术方案.md` |
| F-010 | 逐条已读与通知实时性 | 当前只有“全部已读”，没有单条已读接口；未读角标依赖前端 30 秒轮询，不是实时推送 | 通知点击后仍可能保持未读；PRD“实时可见”只能近似满足 | `NotificationController.java` 只有 3 个端点；`useUnreadNotifications.ts` |

### 2.2 已实现但验证未闭环的功能

| 编号 | 项目 | 当前状态 | 问题 |
|---|---|---|---|
| V-001 | 移动端 / 窄屏 | 已有响应式代码，部分页面做过 655px 走查 | 375×812 真机、触控、软键盘、完整断点矩阵仍未人工验证；W-03 补偿③ / A3-6 仍开放 |
| V-002 | 桌面端视觉走查 | CR-052 做过 655px 宽度走查 | ≥768px 桌面横排布局未完整目视；截图归档只覆盖早期 6 个页面，后续 `/favorites`、`/search`、`/notifications` 未补齐 |
| V-003 | 真实栈回归 | 单测和前端构建通过 | 当前测试没有完整 Spring 上下文测试；数据库、Redis、Flyway、过滤器链和浏览器链路仍需持续真机验证 |

## 3. 已确认的代码 Bug 与技术风险

### 3.1 高优先级

#### BUG-001：楼层点赞存在跨帖子操作风险（已修复）

**状态：✅ 已由 CR-059 于 2026-09-17 修复。**

- 原问题：`ReplyController.like` 接收了路径参数 `postId`，但调用 `toggleReplyLike` 时没有传递或校验它。
- 原问题：application 层只按 `replyId` 查询回复，允许跨帖子拼接路径执行点赞。
- 修复结果：现在先校验父帖可见，再校验回复 `postId` 与路径 `postId` 匹配；不匹配统一返回 `NOT_FOUND / 3001`，不触发点赞、计数或通知。

证据：`ReplyController.java:77-81`、`InteractionApplicationService.java:48-57`、CR-059 实施方案。

验证结果：

1. 同帖点赞正常；
2. 跨帖子回复返回 3001，且不触发 toggle / 计数 / 通知；
3. 父帖不可见返回 3001，且不查询回复；
4. `mvn verify` 187/187 全绿。

#### BUG-002：已下架回复仍可能被点赞或采纳

`ReplyRepositoryImpl.findById` 只过滤 `is_deleted=false`，没有过滤 `status='PUBLISHED'`。而以下两个写用例直接使用它：

- `InteractionApplicationService.toggleReplyLike`：可操作状态为 `REMOVED` 的回复；
- `PostApplicationService.acceptReply`：可能把状态为 `REMOVED` 的回复作为最佳答案。

列表查询虽然过滤了 `status='PUBLISHED'`，但写操作的可见性口径没有复用，形成“前台看不到、仍可操作”的不一致。

证据：`ReplyRepositoryImpl.java:42-46`、`InteractionApplicationService.java:48-52`、`PostApplicationService.java:54-63`。

建议修复：为回复读侧统一定义 `findVisibleById`，同时约束父帖 `PUBLISHED && is_deleted=0`；采纳和点赞都只允许可见回复。

#### BUG-003：生产环境默认配置不安全

`application.yml` 在没有环境变量覆盖时会启用：

- JWT 默认签名密钥；
- AES / HMAC 默认密钥；
- 固定验证码 `123456`；
- 学籍核验旁路 `bypass=true`；
- 本地数据库默认账号密码。

当前代码只打印 WARN，没有阻止应用以这些配置启动。只要部署环境漏配变量，系统可能在“看起来启动成功”的状态下运行在弱安全模式。

证据：`backend/src/main/resources/application.yml:67-94`。

建议修复：按 profile 分离开发与生产配置；生产 profile 启动时强制校验密钥、`bypass=false`、固定验证码为空、`CODE_SENDER_MODE=mail`，不满足即启动失败。

### 3.2 中优先级

#### BUG-004：前端没有处理 token 过期后的统一退出

Pinia 只根据 `localStorage` 中是否存在 `cl_token` 判断登录态，没有保存或检查 `expiresAtEpochSecond`；路由守卫因此会把已过期 token 当成登录状态。后续接口返回 401 时，`client.ts` 只抛出 `ApiError`，没有清理会话、跳转登录或保留回跳地址。

表现：用户打开已过期的浏览器页面时可以进入受保护路由，页面随后显示接口错误，而不是统一回到登录页。

证据：`frontend/src/stores/auth.ts`、`frontend/src/router/index.ts:20-31`、`frontend/src/api/client.ts`。

#### BUG-005：正文长度实现与 PRD 不一致

PRD F-FORUM-002 定义正文上限为 20,000 字，但后端 `PublishPostCommand` 和 `PublishReplyCommand` 均允许 50,000 字，前端 textarea 也允许 50,000 字。

这不是单纯文案问题：接口验收、数据库容量估算、机审耗时和性能指标都会受影响。

证据：PRD F-FORUM-002、`PublishPostCommand.java:7-10`、`PublishView.vue`。

建议：产品确认统一为 20,000 或更新 PRD / 测试 / 性能预算，不能长期两套口径并存。

#### BUG-006：内容安全文案与实际行为不一致

发帖页明确展示“发布前将进行内容安全机审”，但当前发帖 / 回帖服务没有机审调用。该问题会导致用户对平台安全能力产生错误预期，也容易在验收时被误判为已完成。

证据：`PublishView.vue`、`PostApplicationService.java`、`ReplyApplicationService.java`。

#### BUG-007：热榜锁配置存在静默降频风险

配置注释要求锁 TTL 不应大于刷新周期，但代码没有启动校验。若锁 TTL 大于刷新周期，上一轮锁未过期时下一轮会直接跳过，实际刷新周期可能变成 TTL，且不会报错。

证据：`application.yml:95-105`、`AppProperties.Hot`、`HotScoreRefreshScheduler`。

建议：增加配置校验，约束 TTL、刷新周期、窗口天数和批大小的合法范围。

#### BUG-008：互动计数使用字符串拼接 SQL

`adjustLikeCount`、`adjustFavoriteCount` 和 `ReplyRepositoryImpl.adjustLikeCount` 使用 `setSql("... + " + delta)`。目前调用方只传 `±1`，暂未形成可利用输入，但这种写法把安全约束藏在调用约定里，后续扩展容易引入非法 SQL 表达式。

建议：改为固定的 `+ 1` / `- 1` 分支或使用参数绑定，并增加计数不低于 0 的保护。

## 4. 测试、工程与流程问题

| 编号 | 问题 | 当前事实 | 风险 |
|---|---|---|---|
| E-001 | 缺少完整上下文测试 | 当前没有 `@SpringBootTest` / `@WebMvcTest`；187 个测试主要是领域、应用服务、Mock 和结构守护测试 | Flyway、Mapper 扫描、Redis、过滤器链、真实 JSON 和 Spring Bean 装配问题仍可能只在启动或真机暴露 |
| E-002 | 无覆盖率门禁 | `backend/pom.xml` 未接入 JaCoCo，无法验证总覆盖率和核心模块覆盖率 | 测试数量多不代表关键分支被覆盖 |
| E-003 | CI 无静态扫描 | `.github/workflows/backend-ci.yml` 只有 `mvn verify`；没有 Checkstyle、SpotBugs、PMD、Sonar 或依赖漏洞扫描 | 代码风格、潜在缺陷和依赖风险没有自动门禁 |
| E-004 | Code Review 未闭环 | W-05 仍为部分完成，项目没有第二角色批准记录 | 高风险修改主要依赖单人和 AI 自查 |
| E-005 | 设计门仍未闭环 | A3-6 UI 走查、A3-8 部署与机审选型仍开放；W-02 / W-07 关闭条件具备但仍待发起人签署 | 不能把当前状态描述为“设计门通过”或“已具备上线条件” |
| E-006 | 提测准入仍不满足 | `docs/开发/Sprint1计划.md` 记录覆盖率、静态扫描、Code Review、CI 扫描环节未满足 | 当前不具备正式提测准入结论 |
| E-007 | 文档存在状态漂移 | `docs/设计/UI规范/界面规范.md` 仍有“highlight.js 未实现”的正文旧表述，但当前 `package.json`、`utils/highlight.ts` 和 `hljs.css` 已实际落地；CR-058 的提交后核对等文字也有历史残留 | 后续会话容易按过时文档重复实现或误判功能状态 |
| E-008 | 工作区存在未收口改动 | 当前 `CampusLinkApplication.java` 有一处未提交空白变更；`CR-057-热榜定时任务与热门排序.md` 是未跟踪文件，而实际实施使用的是 CR-058 | 容易把旧方案、当前实现和提交凭证混在一起；需由发起人决定保留、归档或删除，删除动作须单独确认 |

## 5. 不应重复误报为现存问题的事项

以下能力本次核对确认已经存在，不再按“未完成”登记：

- `ArchitectureGuardTest`：11 个架构守护用例已通过；
- 路径级鉴权：`EndpointAuthorizationManager` 已替代 `permitAll()` 兜底；
- Markdown XSS 过滤：服务端 `MarkdownRenderer` 有回归测试；
- 前端代码高亮：`highlight.js` 已按需加载，`v-highlight` 已接入帖子详情和楼层；
- 全库时间戳时区修复：CR-051 已完成；
- Sprint 3 已完成的采纳、点赞收藏、站内搜索、通知中心、热榜后端；
- 前端构建和后端 187 个现有测试当前均通过。

## 6. 建议处理顺序

1. 继续修复 `BUG-002`，补“已下架回复、已删除回复”测试，避免继续积累脏互动数据。
2. 把生产安全配置改为启动即校验，确保固定验证码、学籍旁路和开发密钥不能误带入共享或生产环境。
3. 明确 P0 范围：内容机审、举报处置、管理员后台、账号注销、帖子删除、个人主页、标签闭环；每项先补实施方案和接口契约。
4. 补首页“最新 / 热门”切换，并让前端 `listPosts` 显式传 `sort`。
5. 获取真实名册后执行 `bypass=false` 的注册、重复注册、导入和异常回滚全链路回归。
6. 增加最小集成测试：Spring 上下文启动、Flyway、Mapper 扫描、SecurityFilterChain、MySQL / Redis 真实连接；再接入覆盖率和静态扫描。
7. 完成桌面逐页走查和移动端重新排期；补齐新页面截图，并关闭 A3-6。
8. 清理文档状态漂移，并处理 CR-057 未跟踪方案与当前工作区改动。

## 7. 本次审计边界

本次完成了仓库代码、配置、测试、前端路由 / 页面、PRD、技术方案、变更台账、下一步行动和流程偏离记录的静态核对，并执行了后端单测和前端构建。未在本次会话中重新启动完整 MySQL / Redis / Spring / Vite 栈，也未重新执行浏览器逐页人工走查；因此第 2.2 节的真机与视觉问题仍需后续运行态验证。
