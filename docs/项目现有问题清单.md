# Campus-Link 项目现有问题清单

| 文档信息 | 内容 |
|---|---|
| 文档版本 | v1.13 |
| 快照日期 | 2026-09-17（审计基准日；后续修复只加注、不重排历史条目） |
| 最后更新 | 〔**v1.13（2026-09-19）＝[CR-071](变更日志/变更台账.md#cr-071) 个人中心第一轮实施轮：两行回置 + 一行加注、零新登记条目**：① **F-003「个人主页」按视角拆开重述**——**本人视角四格已交付**（`PUT /users/me` + `GET /posts/mine` + `GET /replies/mine` + `/settings` 三区块 + `/me/posts` + `/me/replies`），⚠️ **本行不勾销**，因剩下三样一个没做（他人主页 `/u/:id` 仍是 `Placeholder.vue`、发帖时间线无对外端点、获赞数与被采纳数无聚合接口）；② **F-002「账号注销」加注不移动**——设置页那格是 **`disabled` 按钮 + tooltip 占位**，后端仍零端点，"页面上有按钮"不等于"能注销"；③ **E-013 加"候选 ① 已落地"注、本条不关闭**（`FieldRules` 一份禁则同时供注册与编辑；⚠️ 机审 ② 与 AGENTS 净化断言 ③ 均未做，**禁则字符集 ≠ 机审**）。**本版有一处值得单说的事：一条"新发现"当场撤回、未登记**——核对 `V3` 迁移时见 `flyway_schema_history.installed_on` 与业务表时间差 8 小时，一度判为 [CR-051](变更日志/变更台账.md) 的 UTC 口径在系统表上失效并拟登记 **E-015**，`information_schema` 实测该列是 **`timestamp`**（内部存 UTC、读时按会话时区换算）⇒ **那是正确行为不是缺陷**；教训（CLI 核时间口径须先 `SET SESSION time_zone='+00:00'`）写在[方案 §8](开发/实施方案/CR-071-个人中心第一轮.md)。**为什么留这句而不是当作没发生**：它长得很像一条真缺陷、且与本清单 L-6 的症状同源，后续会话极可能"再发现一次"。**单测基线由 249 升至 294**（§1 第 1 条的 187 / 192 / 208 一串历史值仍只作时点快照）〕；〔**v1.12（2026-09-19）＝七份增量 PRD 评审轮（[CR-070](变更日志/变更台账.md#cr-070)）新增一条决策缺口、零既有条目状态移动**：见 [增量PRD评审纪要](评审/增量PRD评审纪要.md)。**新登记 E-014**——"大模型 / embedding 服务商"是一条**从未被登记过**的决策项（四份 AI 草案依赖它，而 A3-8 只覆盖"部署形态 + 内容机审服务商"、技术方案 grep `LLM|embedding|向量|module/ai` 零命中、ADR 无一条涉及）。⚠️ 该行同时钉住一条容易被读歪的判据：**A3-8 判的是内容进路、AI 服务商是 UGC 出路，签了 A3-8 不等于 AI 依赖已解决**。**为什么本轮只新增一条、不动任何一行**：评审改变的是"哪些草案能不能排期"（结论在纪要 §1），**不是**任何一条现存缺陷的状态——F-005 机审、F-007 标签闭环、F-003 个人主页、E-012 / E-013 的定性**全部原样**（其中 E-013 的处置随 CR-071 实施轮一并做，该行注已写明）。**零代码 / 零测试 / 零契约**（单测仍 **249**、`openapi.json` 未触碰）〕；〔**v1.11（2026-09-19）＝签署轮的一处加注、不移动任何条目**：发起人指示「提交，全部签署」（[CR-069](变更日志/变更台账.md#cr-069)）⇒ **[W-02 与 W-07](流程偏离记录.md) 正式关闭**、W-01 / W-03 / W-04 / **W-05** 只重申接受（**四条前置本身未成立**）。⚠️ **本清单零条目状态变化**：**E-002 / E-003 / E-004（= W-05 的三项硬指标）与 E-005（A3-6 / A3-8）一律未动**——签署轮对它们的影响只有一句"**签字批准了偏离，没有补齐任何指标**"；本版**只给 E-005 行加一条注**（原句"仍待发起人签署"保留为 09-19 时点的陈述，其后加注它已被取代而本行定性不变），另在文件头与 §5 不动其它任何一行。**零代码 / 零测试 / 零契约改动**（单测仍 **249**、`openapi.json` 未触碰〕；〔**v1.10（2026-09-19）＝一处状态加注、不移动任何条目**：[CR-066](变更日志/变更台账.md#cr-066) + [CR-068](变更日志/变更台账.md) 已随 `f58ef73` 提交，本轮补做 `openapi.json` 的**「提交后核对」**（自已提交树重建、临时端口 8097 重抓、与归档快照逐字节一致）⇒ **v1.11 + v1.12 两轮敞口一并闭环，"契约与已提交代码一致"由断言变实测**（见 §5 末条加注、台账 v1.72 / [契约 README](设计/接口契约/README.md) v1.12.1）。⚠️ **本条不改变 E-001 的定性**：快照**至今无机器校验**，"CI 起栈比对"敞口原样留存。**零代码 / 零测试 / 零契约改动**（单测仍 **249**、`openapi.json` 文件零改动）〕 〔**v1.9（2026-09-19）＝[CR-068](变更日志/变更台账.md) 处置 E-011——"仅某角色可调用"这条线自此有机器强制**：`ArchitectureGuardTest` 新增 **G9**（`/api/v1/admin/**` 下的映射方法必须真的调用 `CurrentUser.requireRole`），**两条反证复跑各自成立**（摘掉 `requireRole` → G9 与 2 个 403 用例同红；新加"只调 `requireId`"的 admin 探针 → **G7 仍绿、G9 独红**），故 E-011 由"只登记不处置"转 ✅，**本清单仅移动 E-011 一行**（另在 E-001 与 §5 末行按"只加注"更新基线数）。⚠️ **两处残余就地写在该行末注、不随闭环消失**：Ⅰ. G9 不守"角色取值得当不当"（那一半归端点自带的 403 用例，约定已入 `AGENTS.md`「测试约定」）；Ⅱ. 候选 ②「`@RequireRole` 让声明与判定同源」未做（须改 `common/web` + `security/`，架构级、另开 CR 先写实施方案）。单测 **248 → 249**、**零生产代码改动、`openapi.json` 未触碰**〕 2026-09-19（**v1.8：起草第 7 份增量 PRD——[F-ACC-007 个人中心与账号设置](需求/产品需求文档-个人中心设置.md)（预留 CR-067、待发起人评审、通过前不得开工）时的取证副产品入册**：① 新登记 **E-012**（主 PRD §4 三格与实测不符——「学校 / 专业为选填」实为**零写入入口**且开发库 53 账号三列全 NULL、该表下方建议补的「JWT 7 天滑动续期」实测**无任何续期代码**、「叠加 CSP 策略」**全仓零命中**）；② 新登记 **E-013**（昵称只有长度校验、无字符集白名单与内容审查，`bio` 一旦落地即再开一个自由文本出口）；③ §6 第 3 条加一条进展注（七项中的"个人主页"自此有了**相邻但不同**的草案，主页本体仍未动）。**本轮零代码 / 零基线改动 / 零契约**——单测仍 **248**、`openapi.json` 未触碰，条目状态除新增上述两条外**一律未移动**；上一版 2026-09-18（**v1.7：[CR-066](变更日志/变更台账.md#cr-066) 内容处置「下架 / 恢复」后端落地（F-SAFE-003 一支、零迁移、四类审计），本清单三处改动**：① F-006 由"未做"回置为 🟡「五件事之一、只有 API」（现状列加更正句、影响列加"第三句不签"注、证据列追加实施结果与取证指针，原 v1.6 注一字未改）；② 新登记流程缺口 **E-011**（"仅 SUPERADMIN"无机器强制——CR-066 反证时实测：摘掉 `requireRole` 后 `ArchitectureGuardTest` 与鉴权矩阵测试全绿，只有控制器 403 用例红），**本条只登记不处置**；③ 两处单测基线按"只加注"更新到 **248**（E-001 行与 §5 末行）。**真机 45 项取证全绿、契约快照 22 → 24 操作**（详见[方案 §7](开发/实施方案/CR-066-内容处置下架与恢复.md)）；⚠️ **F-006 不勾销**（举报 / 工单 / 禁言封禁 / 运营后台四支未动），**F-005 机审仍未做**、**F-SAFE-003 验收第三句"作者收到通知"明确不做**；上一版 v1.6：[CR-066](变更日志/变更台账.md#cr-066) 内容处置「下架 / 恢复」立项——只交付方案、零代码**，故**本清单不移动任何条目状态、不勾销任何一项**：F-006 五件事里的"内容下架"一支自此有方案（`PUT /api/v1/admin/{posts,replies}/{id}/status`、**零迁移**、审计四类），⚠️ **实施后本行也只按"部分"回置**（举报 / 工单 / 禁言封禁 / 运营后台四支未动），且 PRD F-SAFE-003 验收第三句"作者收到通知"**明确不做**（需新增通知 type + 前端文案链，`frontend/` 由并发会话占用）。同批在 F-006 行加一条取证注（**`REMOVED` 与 `replies.status` 至今零写入口**），并把 [CR-064](变更日志/变更台账.md#cr-064) / [CR-065](变更日志/变更台账.md#cr-065) 的**提交凭证 `8216843`（已推送 origin/main）**记在 §5 末；**上一版 v1.5：[CR-065](变更日志/变更台账.md#cr-065) 帖子删除后端落地，F-004 由「未完成」转 🟡「后端已交付、前端无入口」**——`DELETE /api/v1/posts/{id}`（仅作者、写 `is_deleted=1` 墓碑、同事务记 `POST_DELETE` 审计），单测基线 **208 → 220**、真机 38 项取证（其间**抓到收藏页读侧未排墓碑的真实缺陷并当场修**，见 §2.1 F-004 与[方案 §7](开发/实施方案/CR-065-帖子删除.md)）；**F-004 不整体勾销**（前端入口 + PRD"计数回收"两项未做），只改 §1 第 2 条注与 §2.1 该行，其余条目一字未动**；上一版 v1.4：**[CR-063](变更日志/变更台账.md#cr-063) 实施完成，BUG-003 由开放转「✅ 已修复」**——`prod` profile + `ProductionSafetyGuard`（BFPP）+ 守护测试 16 例，单测基线 **192 → 208**，§6 第 2 条勾销、§1 第 4 条最高优先级问题闭环，原描述与上一条 09-18 注一字未改、只追加实施结果；上一版 v1.3：登记 CR-063 后为 BUG-003 与 §6 第 2 条加时效注——方案已备、待发起人确认，本条仍开放；v1.2：[CR-062](变更日志/变更台账.md) 处置 E-007 + 新增同源条目 E-009 + §1 两处过期事实加注；v1.1：BUG-001 / BUG-002 由 CR-059 / CR-060 修复后增补） |
| 维护人 | 项目经理（发起人兼任） |
| 适用范围 | 当前仓库 `main` 分支代码、前端、测试、配置与项目文档 |
| 维护性质 | 问题盘点，不替代 PRD、技术方案、变更台账或流程偏离记录 |

## 1. 结论先行

项目当前已经具备账号注册登录、固定版块、发帖、回帖、采纳、点赞收藏、搜索、通知中心，以及热榜后端能力，且当前静态构建验证通过：

- 后端 `mvn verify`：187 个测试全部通过；〔**2026-09-18 注**：该数为审计时点基线，[CR-060](变更日志/变更台账.md) 后升至 **192**、[CR-063](变更日志/变更台账.md#cr-063) 后升至 **208**（含 `ProductionSafetyGuardTest` 16 例，两次均全绿），与 §5 末条同口径〕
- 前端 `npm run build`：构建通过；
- 当前 HEAD：`286739b`；〔**2026-09-18 注**：已推进——`a8c923f`（CR-059）→ `8b37194`（CR-060 + CR-061）→ **`5ad0ee8`**（快照「提交后核对」与凭证回填），三者均已推送 `origin/main`；本条为审计时点值，保留不改写〕
- 当前仍未达到完整产品交付条件，主要原因不是编译失败，而是 P0 功能缺失、内容安全与运营后台缺失、真实名册链路未验证，以及集成测试和提测门禁不完整。

本次审计确认的最高优先级问题是：

1. 回复点赞接口忽略路径中的 `postId`，且回复写操作没有统一过滤 `status`，存在跨帖子操作和操作已下架回复的风险；〔**2026-09-18 注**：两个风险已分别由 [CR-059](变更日志/变更台账.md)（跨帖子，2026-09-17）与 [CR-060](变更日志/变更台账.md)（可见性过滤，2026-09-18）修复，见 §3.1；本条列出的原样描述保留为审计快照。〕
2. P0 的内容机审、举报、内容处置、管理员后台、账号注销、帖子删除、个人主页等功能尚未完成；〔**2026-09-18 注**：所列七项中**"帖子删除"已转 🟡**——后端由 [CR-065](变更日志/变更台账.md#cr-065) 交付并真机取证，**前端入口未做**（见 §2.1 F-004），其余六项仍未完成；本条原样描述保留为审计快照，不改写。〕〔**2026-09-19 注（[CR-071](变更日志/变更台账.md#cr-071)）：七项里"个人主页"再拆一半**——**本人视角那一半已交付**（资料编辑 `PUT /users/me` + 我的帖子 + 我的回帖 + `/settings` 三区块，前后端均实测），**他人主页 `/u/:id` 仍未做**（路由还指向 `Placeholder.vue`，且"发帖时间线 / 获赞数 / 被采纳数"三个接口一个都没写），故该子项转 🟡「本人视角已交付、他人主页未做」而**不算完成**；⚠️ 同批**"账号注销"一格未动**——设置页那个按钮是 `disabled` + tooltip 占位，**后端没有可提交的端点**，不得读成"注销已实现"；**"内容机审"一格也未动**（本 CR 加的字符集禁则是防仿冒 / 防标签注入，**不是机审**，见 E-013 注）。本条原样描述仍保留为审计快照。〕
3. 首页仍只有“最新”列表，热榜目前只有后端 `sort=hot`，PRD 要求的“最新 / 热门”前端切换未交付；
4. 默认配置仍启用固定验证码、学籍旁路和开发密钥，缺少启动级的生产环境 fail-fast 防护；〔**2026-09-18 已闭环**：[CR-063](变更日志/变更台账.md#cr-063) 引入 `prod` profile + `ProductionSafetyGuard`，漏配即启动失败，详见 §3.1 BUG-003〕
5. 现有测试主要是纯单测，没有完整 Spring 上下文、数据库、Redis 和浏览器回归测试，`mvn verify` 不能代表真实启动和联调通过。

## 2. 已确认的未完成功能

### 2.1 P0 功能缺口

| 编号 | 功能 | 当前状态 | 影响 | 证据 |
|---|---|---|---|---|
| F-001 | 真实学籍名册与正式注册链路 | 真实名册仍未到位，开发依赖 `APP_ROSTER_BYPASS=true` 或合成数据；尚未完成关闭 bypass 后的完整回归 | 注册身份可信性仍未被真实数据验证；B1 / B2 未闭环 | `docs/下一步行动.md`、`docs/开发/Sprint1计划.md`、`backend/src/main/resources/application.yml:87-89` |
| F-011 | 手机号注册 / 登录 | PRD F-ACC-001 定义“邮箱或手机号 + 验证码”，当前 Command、Controller、前端表单和发送逻辑均只支持邮箱；数据库虽预留 `phone_enc` / `phone_hash`，但没有业务链路 | P0 需求只完成邮箱分支，不能声称注册登录完整交付 | PRD F-ACC-001；`AccountCommands.java`；`AccountController.java`；`LoginView.vue` |
| F-002 | 账号注销 | 无注销接口、应用服务、前端入口和冷静期任务〔**2026-09-19 注（[CR-071](变更日志/变更台账.md#cr-071)）**：前端 `/settings` 账号区出现了「注销账号」一格，但它是 **`disabled` 按钮 + tooltip 说明"尚未开放"**，**后端仍零端点、零应用服务、零冷静期任务**——本行**不因"页面上有这个按钮"而回置**；连带一处口径：草案 §3.2 写的「`DEACTIVATED` 允许编辑」本期**无路径可达**（`AccountApplicationService.login` 用 `isActive()`，冷静期账号拿不到令牌），与主 PRD「冷静期可登录」是**真实冲突**，登记在[评审纪要](评审/增量PRD评审纪要.md)与[方案 §7](开发/实施方案/CR-071-个人中心第一轮.md)，**待发起人裁决、本轮未解**〕 | PRD F-ACC-003 为 P0，但用户无法行使注销和撤销权利 | PRD F-ACC-003；当前 `account/web` 无对应映射 |
| F-003 | 个人主页 | `/u/:id` 仍指向 `Placeholder.vue`；后端只有 `/users/me`，没有他人主页、发帖时间线、获赞数、被采纳数等接口〔**2026-09-19 注（[CR-071](变更日志/变更台账.md#cr-071)）：本条按"他人视角 / 本人视角"两半重述**——**本人视角已交付**：`PUT /api/v1/users/me` 资料编辑 + `GET /posts/mine` + `GET /replies/mine` 三端点，前端 `/settings`（资料编辑 / 只读回显 / 我的内容 / 账号四区块）+ `/me/posts` + `/me/replies` 三页与顶栏四入口，`mvn verify` 294 全绿 + 浏览器实测；⚠️ **本条不勾销**，因为格子里剩下的三样一个没做——**他人主页 `/u/:id`（路由仍是 `Placeholder.vue`）、发帖时间线（"我的"两个列表只给本人、无 `/u/{id}/posts`）、获赞数与被采纳数（无聚合接口，`InteractionResult.count` 是单帖总数不是人）**；另**「届」徽章未渲染**，原因不是漏做而是**服务端从不下发 `studentId`**（敏感信息红线），客户端没有推导入料，见[方案 §8 ⑨](开发/实施方案/CR-071-个人中心第一轮.md)〕 | PRD F-ACC-002 未完成（**本人视角部分已交付 ⇒ 🟡**）；个人身份的**对外**展示链路仍断开 | `frontend/src/router/index.ts:13`、PRD F-ACC-002、[F-ACC-007 草案](需求/产品需求文档-个人中心设置.md)（本轮实施四格、注销一格未做） |
| F-004 | 帖子删除 | 🟡 **后端已交付（2026-09-18，[CR-065](变更日志/变更台账.md#cr-065)）**：`DELETE /api/v1/posts/{id}` 已实现为**仅作者**的软删（写 `posts.is_deleted=1` 行级墓碑、零迁移），删后 11 处前台读/写位置逐项实测不可达，同事务记 `POST_DELETE` 审计；**前端仍无删除入口**，且 PRD 的"计数回收"在现库上**没有作用对象**（`boards` / `users` 均无 `*_count` 列），两点均按落码偏差登记 | ⚠️ **本条不整体勾销**：接口侧闭环 ≠ 用户可用，PRD F-FORUM-006 的**用户可见验收不签**；余下缺口是前端入口 + 计数列（后者属独立一批变更） | 原证据"当前 `PostController` 无 `@DeleteMapping`"**已失效**（现为 `PostController#delete`，`@DeleteMapping("/{id}")` + `@SecurityRequirement`）；取证见[实施方案 §7](开发/实施方案/CR-065-帖子删除.md)；PRD F-FORUM-006 |
| F-005 | 内容机审 | 发帖与回帖直接渲染并保存，没有第三方审核 gateway、审核结果处理或 fail-closed 开关 | PRD F-SAFE-001 是 P0；前端“发布前将进行内容安全机审”属于未兑现承诺 | `PostApplicationService.java:38-43`、`ReplyApplicationService.java`；`PublishView.vue` |
| F-006 | 举报、内容处置和运营后台 | `/admin` 仍是占位页；后端只有学籍名册导入，没有举报、工单、内容下架、用户禁言 / 封禁等业务接口〔**2026-09-18 更正（v1.7）**：本句的"没有内容下架"自 [CR-066](变更日志/变更台账.md#cr-066) 起**不再成立**——`PUT /api/v1/admin/posts/{id}/status` 与 `…/replies/{id}/status` 已落地（`ContentModerationApplicationService` + 两个适配器定向 UPDATE + 四类审计），**举报 / 工单 / 禁言封禁 / 运营后台四支仍缺**〕 | PRD F-SAFE-002 / F-SAFE-003 / F-ADMIN-001~003 全部未完成；上线后没有基本运营闭环〔**v1.7 注**：F-SAFE-003 的"下架 / 恢复 + 留审计"两句已交付，**"作者收到通知"一句明确未做**、页面上也无处置入口 ⇒ **本行按 🟡「一支五件之一、只有 API」回置，不勾销**〕 | `frontend/src/router/index.ts:16`；当前 `module` 下无 report / moderation / admin content controller〔**2026-09-18 注（v1.6）**：本行五件事中的**"内容下架"一支已立项**——[CR-066](变更日志/变更台账.md#cr-066) 的可实施方案已写好（`PUT /api/v1/admin/{posts,replies}/{id}/status`，**零迁移**，`status` 列自 V1 就位却**全仓无写入口**），**状态＝方案待发起人确认、本轮零代码，故本行不移动状态、不勾销**。取证侧顺带确认一条事实供后续轮次引用：**`PostStatus.REMOVED` 与 `replies.status` 至今零写入口**（`PostStatus.java:3` 自陈"预留"），这也是 §3.1 里把 BUG-002 定性为"当前不可触发"的同一条依据。作者自助删除（[CR-065](变更日志/变更台账.md#cr-065)）属 **F-004**、不计入本行〕〔**2026-09-18 实施结果（v1.7）**：上面那句"至今零写入口"**已失效**——两处写入口由 [CR-066](变更日志/变更台账.md#cr-066) 补上（原句一字保留为当时快照）。实测口径见[方案 §7](开发/实施方案/CR-066-内容处置下架与恢复.md)：**真机 45 项全绿**（下架后十二处不可见、`status` 写 `REMOVED` 而 `is_deleted` 仍 0、作者不能借自助删除绕过处置、幂等不增审计行、热榜下一轮置 0）；**单测 220 → 248**；契约快照 22 → **24 操作 / 15 受保护**、纯新增已机器核对。⚠️ 同批**顺带修掉一条既有读侧缺陷**（通知条目对已下架 / 已删帖仍下发可点 `postId`，前端注释里那句契约服务端从未履行），并新登记一条流程缺口 **E-011**（"仅 SUPERADMIN"无机器强制）〕 |
| F-007 | 标签闭环 | 数据库有 `tags`、`post_tags` 和 `posts.tags`，但发帖 Command、前端表单和持久化链路都没有标签入参；搜索只能对空的 `posts.tags` 做 LIKE 兜底 | “按标签搜索”实际上没有真实数据来源；PRD F-FORUM-002 / F-FORUM-008 只完成标题搜索 | `PublishPostCommand.java` 无 tags 字段；`frontend/src/api/forum.ts:85-87`；`PostMapper.java` |
| F-012 | 发帖实时预览 | 发帖页右侧明确显示“暂无实时预览”，前端没有 Markdown 预览状态或预览组件 | PRD F-FORUM-002 的实时预览验收项未完成；用户只能发布后查看结果 | `frontend/src/views/PublishView.vue`；PRD F-FORUM-002 |
| F-008 | 首页“最新 / 热门”切换 | 后端已支持 `GET /api/v1/posts?sort=hot`，首页固定调用默认最新排序，没有 tab / segmented control，也没有把 `sort` 传入 API | PRD F-FORUM-003 只交付后端一半，用户无法从首页使用热榜 | `HomeView.vue:19-40`、`frontend/src/api/forum.ts:71-78` |
| F-009 | 回复引用 | `quoted_reply_id` 虽在 V1 表中预留，但回帖 Command、UI 和业务服务均未实现；通知类型 `quote` 也未实现 | PRD F-FORUM-004 / F-SOC-001 的引用回复场景缺失 | `PublishReplyCommand.java`、`NotificationType.java`、`docs/设计/技术方案.md` |
| F-010 | 逐条已读与通知实时性 | 当前只有“全部已读”，没有单条已读接口；未读角标依赖前端 30 秒轮询，不是实时推送 | 通知点击后仍可能保持未读；PRD“实时可见”只能近似满足 | `NotificationController.java` 只有 3 个端点；`useUnreadNotifications.ts` |

### 2.2 已实现但验证未闭环的功能

| 编号 | 项目 | 当前状态 | 问题 |
|---|---|---|---|
| V-001 | 移动端 / 窄屏 | 已有响应式代码，部分页面做过 655px 走查 | 375×812 真机、触控、软键盘、完整断点矩阵仍未人工验证；W-03 补偿③ / A3-6 仍开放 |
| V-002 | 桌面端视觉走查 | CR-052 做过 655px 宽度走查 | ≥768px 桌面横排布局未完整目视；截图归档只覆盖早期 6 个页面，后续 `/favorites`、`/search`、`/notifications` 未补齐 |
| V-003 | 真实栈回归 | 单测和前端构建通过 | 当前测试没有完整 Spring 上下文测试；数据库、Redis、Flyway、过滤器链和浏览器链路仍需持续真机验证 |

## 3. 已确认的代码 Bug 与技术风险

### 3.1 高优先级

#### BUG-001：楼层点赞存在跨帖子操作风险（已修复）

**状态：✅ 已由 CR-059 于 2026-09-17 修复。**

- 原问题：`ReplyController.like` 接收了路径参数 `postId`，但调用 `toggleReplyLike` 时没有传递或校验它。
- 原问题：application 层只按 `replyId` 查询回复，允许跨帖子拼接路径执行点赞。
- 修复结果：现在先校验父帖可见，再校验回复 `postId` 与路径 `postId` 匹配；不匹配统一返回 `NOT_FOUND / 3001`，不触发点赞、计数或通知。

证据：`ReplyController.java:77-81`、`InteractionApplicationService.java:48-57`、CR-059 实施方案。

验证结果：

1. 同帖点赞正常；
2. 跨帖子回复返回 3001，且不触发 toggle / 计数 / 通知；
3. 父帖不可见返回 3001，且不查询回复；
4. `mvn verify` 187/187 全绿。

#### BUG-002：已下架回复仍可能被点赞或采纳（已修复）

**状态：✅ 已由 CR-060 于 2026-09-18 修复。**

- 原问题：`ReplyRepositoryImpl.findById` 只过滤 `is_deleted=false`，没有过滤 `status='PUBLISHED'`。而以下两个写用例直接使用它：
  - `InteractionApplicationService.toggleReplyLike`：可操作状态为 `REMOVED` 的回复；
  - `PostApplicationService.acceptReply`：可能把状态为 `REMOVED` 的回复作为最佳答案。
- 原问题：列表查询虽然过滤了 `status='PUBLISHED'`，但写操作的可见性口径没有复用，形成“前台看不到、仍可操作”的不一致。

**修复结果**：读侧方法整体替换为 `findVisibleById`（**删除** `findById`——取证它全仓恰有这两个调用方，替换后零调用方，不留两种读法并存），SQL 侧同时过滤 `status='PUBLISHED' AND is_deleted=0`，与列表方法 `findPageByPostId` 同源；两条写用例改用它核验目标，不可见一律 `404 / 3001`（沿用不区分原因的防枚举口径）。守卫**只放在读侧一处**，`adjustLikeCount` 不重复表达同一条件（两处即两个事实源；读→写之间的 TOCTOU 登记为已知残余）。

**严重性定性（方案阶段取证，与清单原归类不同）**：⚠️ 该缺陷**当前不可触发**——全仓无任何写 `replies.status` 的代码（机审 F-SAFE-001 / 处置台 F-SAFE-003 未实现，`ReplyConverter` 不写该列、由 DB 默认 `'PUBLISHED'`），`REMOVED` 只能人工改库产生。故本条实为**处置台落地前的前置防线**，严重性据实由 §3.1 的“高优先级”**下调为中**（保留在本节、只加注，不重排历史条目）。

证据：`ReplyRepository.java`（`findVisibleById` javadoc）、`ReplyRepositoryImpl.java`、`InteractionApplicationService.java`、`PostApplicationService.java`、CR-060 实施方案 §7。

验证结果：

1. 单测 **187 → 192** 全绿（新增 `ReplyRepositoryImplVisibleTest` 3 例 + 应用层 2 例零副作用断言）；
2. 反证实验（临时摘掉 `status` 条件）：`visibleQueryCarriesBothConditions` 与 `writeSideFilterMatchesListFilter` **两例同时失败**，复原后全绿——守卫确由机器强制；
3. 真机取证 **17/17**：置 `REMOVED` 的楼层点赞 / 采纳均 `404 / 3001`，`replies.like_count`、`likes` 关系行、`posts.accepted_reply_id`、`posts.is_accepted`、`replies.is_accepted`、楼层作者通知**全部零变化**；列表不再出现该楼层（读写同源实测成立）、帖子详情未误伤；复原 `PUBLISHED` 后同一条楼层仍可点赞与采纳，匿名点赞仍 `401 / 4001`；
4. `openapi.json` 重抓后**零 diff**（接口形状、错误码、鉴权声明均未变）。

#### BUG-003：生产环境默认配置不安全（已修复）

**状态：✅ 已由 [CR-063](变更日志/变更台账.md#cr-063) 于 2026-09-18 修复并验证。** 下面的原描述与那条 09-18 注**一字未改**，只在文末追加实施结果。

`application.yml` 在没有环境变量覆盖时会启用：

- JWT 默认签名密钥；
- AES / HMAC 默认密钥；
- 固定验证码 `123456`；
- 学籍核验旁路 `bypass=true`；
- 本地数据库默认账号密码。

当前代码只打印 WARN，没有阻止应用以这些配置启动。只要部署环境漏配变量，系统可能在“看起来启动成功”的状态下运行在弱安全模式。

证据：`backend/src/main/resources/application.yml:67-94`。

建议修复：按 profile 分离开发与生产配置；生产 profile 启动时强制校验密钥、`bypass=false`、固定验证码为空、`CODE_SENDER_MODE=mail`，不满足即启动失败。

〔**2026-09-18 注（[CR-063](变更日志/变更台账.md)）**：已按本条原建议写出可实施方案并登记台账、**待发起人确认后才动代码**——方案见 [`docs/开发/实施方案/CR-063-生产安全配置启动即校验.md`](开发/实施方案/CR-063-生产安全配置启动即校验.md)。**三条取证事实补在原描述之上**：**① 项目今天根本没有"生产"这个概念**——全仓 `@Profile` 与 `Environment` 判定**零处**，`${VAR:dev-default}` 的默认值就是唯一生效值，故"漏配变量"不是边缘情况、而是**当前默认路径**；**② 现有的 `CryptoService.requireKey` 只断言"≥32 字节"**，而上述三把开发默认密钥**长度全都够**，因此它对"用了仓库里的密钥"这一情形**完全无感**——"已经有校验"不等于"校验在点上"；**③ `InMemoryTestRosterGateway` / `DevAdminAccountSeeder` / `LogCodeSender` 三处 `@ConditionalOnProperty(matchIfMissing = true)`** 在属性整体缺失时选中的都是**开发实现**（fail-open），方案按"prod 显式赋值 + 守卫断言"绕过并把真正的修法列为**已知残余**。**本条状态仍为开放**（未实施、未验证），不并入 §5 的"不应重复误报"清单。〕

〔**2026-09-18 实施结果（[CR-063](变更日志/变更台账.md#cr-063)，本条据此关闭）**：按上面那条"建议修复"的字面口径落地——**新增 `application-prod.yml`**（本仓第一个 profile 文件：三个开发开关写死为 `""` / `false` / `mail`，五处凭据不留默认值）＋ **新增 `config/ProductionSafetyGuard`**（`@Profile("prod")`，8 项校验**一次列全违规**再抛异常，通过时只记 SHA-256 前 8 位指纹、不落密钥明文）＋ **`ProductionSafetyGuardTest` 16 例**（含 2 例配置防漂移：guard 清单须与 `application.yml` 默认值逐字相等、prod 文件须仍不含 `:default`）。**验证**：`mvn -o verify` **192 → 208 全绿**；反证 **6/6 皆红**（摘 4 条校验 + 造 2 处配置漂移）；**prod 真机三轮**——A 轮不设任何环境变量 → 第一条异常即「生产安全配置校验未通过（4 项）」逐条点名，B 轮只给 `JWT_SECRET` → 3 项，C 轮全给值 → **先**出「校验通过：8 项」**再**在连库阶段失败（证明校验位于链路最前）；**dev 零回归**（health 200 / 列表 200 / 匿名发帖 401 / 热榜 200，启动日志不含守卫字样）；`openapi.json` **零 diff**。**两条被实测推翻的常识，值得留在台账里**：Ⅰ. 守卫**必须是 `BeanFactoryPostProcessor`**——写成 `InitializingBean` 时它排在数据源 / Flyway / `JwtService` 之后，真机三轮**一次都没说话**，问题会被"看起来修好了"掩盖；Ⅱ. **"占位符不留默认值即启动失败"不成立**——Boot 绑定 `@ConfigurationProperties` 走非严格解析，把 `${DB_USER}` 字面量原样注入（真机报的是 MySQL `Access denied for user '${DB_USER}'`），所以"零代码的第一道防线"是漏的，实际拦截点只有守卫。**三条残余如实登记、未粉饰**：三处 `matchIfMissing = true` 的 fail-open 原样保留（改它要动默认 profile 装配路径）；本仓仍无 Spring 上下文级测试，"prod 真的会拦"只有真机取证支撑；`application-prod.yml` 若被误删，Spring 会静默按默认配置启动（属打包期问题）。⚠️ 另须知情：yml 里写死字面量**只消灭"默认值可回退"，不消灭覆盖本身**（`CAMPUSLINK_CAPTCHA_FIXED_CODE=123456` 这类环境变量或命令行参数照样盖得过 yml）。**本条关闭的边界**：只关"漏配能否启动"，**不闭环 A3-8**（部署与机审选型）、不动提测准入门任何一项、`W-*` 一律不签。〕

### 3.2 中优先级

#### BUG-004：前端没有处理 token 过期后的统一退出

Pinia 只根据 `localStorage` 中是否存在 `cl_token` 判断登录态，没有保存或检查 `expiresAtEpochSecond`；路由守卫因此会把已过期 token 当成登录状态。后续接口返回 401 时，`client.ts` 只抛出 `ApiError`，没有清理会话、跳转登录或保留回跳地址。

表现：用户打开已过期的浏览器页面时可以进入受保护路由，页面随后显示接口错误，而不是统一回到登录页。

证据：`frontend/src/stores/auth.ts`、`frontend/src/router/index.ts:20-31`、`frontend/src/api/client.ts`。

#### BUG-005：正文长度实现与 PRD 不一致

PRD F-FORUM-002 定义正文上限为 20,000 字，但后端 `PublishPostCommand` 和 `PublishReplyCommand` 均允许 50,000 字，前端 textarea 也允许 50,000 字。

这不是单纯文案问题：接口验收、数据库容量估算、机审耗时和性能指标都会受影响。

证据：PRD F-FORUM-002、`PublishPostCommand.java:7-10`、`PublishView.vue`。

建议：产品确认统一为 20,000 或更新 PRD / 测试 / 性能预算，不能长期两套口径并存。

#### BUG-006：内容安全文案与实际行为不一致

发帖页明确展示“发布前将进行内容安全机审”，但当前发帖 / 回帖服务没有机审调用。该问题会导致用户对平台安全能力产生错误预期，也容易在验收时被误判为已完成。

证据：`PublishView.vue`、`PostApplicationService.java`、`ReplyApplicationService.java`。

#### BUG-007：热榜锁配置存在静默降频风险

配置注释要求锁 TTL 不应大于刷新周期，但代码没有启动校验。若锁 TTL 大于刷新周期，上一轮锁未过期时下一轮会直接跳过，实际刷新周期可能变成 TTL，且不会报错。

证据：`application.yml:95-105`、`AppProperties.Hot`、`HotScoreRefreshScheduler`。

建议：增加配置校验，约束 TTL、刷新周期、窗口天数和批大小的合法范围。

#### BUG-008：互动计数使用字符串拼接 SQL

`adjustLikeCount`、`adjustFavoriteCount` 和 `ReplyRepositoryImpl.adjustLikeCount` 使用 `setSql("... + " + delta)`。目前调用方只传 `±1`，暂未形成可利用输入，但这种写法把安全约束藏在调用约定里，后续扩展容易引入非法 SQL 表达式。

建议：改为固定的 `+ 1` / `- 1` 分支或使用参数绑定，并增加计数不低于 0 的保护。

## 4. 测试、工程与流程问题

| 编号 | 问题 | 当前事实 | 风险 |
|---|---|---|---|
| E-001 | 缺少完整上下文测试 | 当前没有 `@SpringBootTest` / `@WebMvcTest`；192 个测试（2026-09-18 基线，原记 187；〔同日 v1.7 注：CR-065 后 220、**CR-066 后 248**——本行"数量增加但仍是纯单测"的判断不随基线变化而改变；**2026-09-19 v1.9 注：[CR-068](变更日志/变更台账.md) 后 249**（新增的 1 例正是 `ArchitectureGuardTest` 的 G9，仍属纯单测、无需 Spring 上下文）〕）主要是领域、应用服务、Mock 和结构守护测试 | Flyway、Mapper 扫描、Redis、过滤器链、真实 JSON 和 Spring Bean 装配问题仍可能只在启动或真机暴露 |
| E-002 | 无覆盖率门禁 | `backend/pom.xml` 未接入 JaCoCo，无法验证总覆盖率和核心模块覆盖率 | 测试数量多不代表关键分支被覆盖 |
| E-003 | CI 无静态扫描 | `.github/workflows/backend-ci.yml` 只有 `mvn verify`；没有 Checkstyle、SpotBugs、PMD、Sonar 或依赖漏洞扫描 | 代码风格、潜在缺陷和依赖风险没有自动门禁 |
| E-004 | Code Review 未闭环 | W-05 仍为部分完成，项目没有第二角色批准记录 | 高风险修改主要依赖单人和 AI 自查 |
| E-005 | 设计门仍未闭环 | A3-6 UI 走查、A3-8 部署与机审选型仍开放；W-02 / W-07 关闭条件具备但仍待发起人签署〔**2026-09-19 v1.11 注（[CR-069](变更日志/变更台账.md#cr-069) 签署轮）**：上句后半段"**仍待发起人签署**"已不成立——发起人指示「全部签署」，**W-02 与 W-07 已正式关闭**（见[流程偏离记录](流程偏离记录.md) v1.11 两条的「关闭签署（2026-09-19）」行）。⚠️ **但本行定性一字不改、仍开放**：**A3-6 与 A3-8 两项跟 W-02 / W-07 的关闭前置无关**（W-02 的前置是 A3-9 / A3-10 / A3-11，三条均已履行），**设计门整体仍是"有条件通过"（11 项闭环 9）**，"不能把当前状态描述为设计门通过或已具备上线条件"这一条风险**依旧成立**。同批如实登记的另有两条：本轮**未**签署关闭 W-01 / W-03 / W-04 / W-05（四条前置本身未成立，其中 W-05 恰好覆盖本清单 **E-002 / E-003 / E-004** 三行——**签署轮对这三行零改动**）〕 | 不能把当前状态描述为“设计门通过”或“已具备上线条件”〔**v1.11 注**：签署轮之后这句**更重要**，不是更轻：**签字批准了偏离，没有补齐任何指标**〕 |
| E-006 | 提测准入仍不满足 | `docs/开发/Sprint1计划.md` 记录覆盖率、静态扫描、Code Review、CI 扫描环节未满足 | 当前不具备正式提测准入结论 |
| E-007 | 文档存在状态漂移（**✅ 已处置，2026-09-18，[CR-062](变更日志/变更台账.md)**） | 原记的两处均已收口：① `docs/设计/UI规范/界面规范.md` 的 **4 处**"highlight.js 未引入 / 属 ADR-005 欠账"旧表述（§1 事实表、§3 第 8 项、§5.6 表、§9.1 P0 表）已按该文件既有的**"原句一字保留 + 其后加注"**写法回填为已落地事实，本文升 **v1.8**（`package.json` `^11.12.0`、`utils/highlight.ts` 的 `v-highlight` + 按需注册 29 种语言、`styles/hljs.css` 令牌映射、`PostDetailView.vue` 两处调用点，均随 `286739b` 提交）；② "CR-058 的提交后核对等文字也有历史残留"——已于同批完成，[接口契约 README](设计/接口契约/README.md) 升 **v1.10.1**，快照代码来源由"未提交的工作区"更正为 `286739b` + `8b37194`（自 8097 实例重抓与仓库快照逐字节一致、`git diff` 为空） | 原风险（后续会话按过时文档重复实现已存在的能力）**已消除**；⚠️ 但**同族问题并未根除**——本次修 E-007 时又核出一份文档的同源漂移，登记为 **E-009**（下方），根因是"代码交付了、文档落点清单没执行"这一流程缺口，**不是单份文档的偶发笔误** |
| E-008 | 工作区存在未收口改动（**两项均已处置，2026-09-18**） | 原记的两处：① `CampusLinkApplication.java` 的未提交空白变更——现 `git status` 对该文件**无输出**，工作区干净；② 未跟踪的作废方案 `CR-057-热榜定时任务与热门排序.md`——**经发起人 2026-09-18 明确同意已删除**（删除前逐行比对确认它是 CR-058 方案的早期稿、除编号与一处旧锚点外**无独有内容**，且全仓无 markdown 链接指向该路径）。本轮工作区仍留有 CR-060 / CR-061 的未提交改动，属**正常待提交状态**、不再列为本条。〔**2026-09-18 同版时效注**：上句"CR-060 / CR-061 未提交"已过期——两者已提交为 **`8b37194`** 并推送，`5ad0ee8` 为其快照核对与凭证回填；**此刻未提交的是 [CR-062](变更日志/变更台账.md) 的纯文档改动**，同一风险按同一口径处理（提交时以 commit message 的 CR 号对齐即可）〕 | 原风险（旧方案与现行实现混读）已消除；**剩余风险仅在于"改动未提交即无凭证"**，由发起人提交时按 CR 号对齐 |
| E-009 | 文档状态漂移（同源第二例）：`docs/设计/UI规范/前端界面素材选型.md` 的 CR-055 批次标记过时 | 该文档 §1 事实基线与 §2 判定表把 **Tabler 图标 / 中文字体 / 头像占位** 三行标为"⏳ 选型已定、**尚未落地**"（另见 §0 读表警示与 §2 `cn-fontsource` 行的"批次④ 待落地"），而三者代码**均已随 `286739b` 落地**：`components/SvgIcon.vue` + `assets/icons/` **18 个** Tabler SVG（`import.meta.glob` + `?raw`，零构建依赖）、`components/UserAvatar.vue`、`main.ts` 引入 `@fontsource-variable/noto-sans-sc`。**另有两处实现与方案的实质差异未被记录**：① 字体实际取自 **`@fontsource-variable/noto-sans-sc`**（Fontsource 的变量字族，woff2 分片 + `unicode-range`），**不是**方案与本文写的"中文网字计划 `cn-fontsource`"；② **本地 `04-头像占位/` 的 8 个 SVG 并未入仓**（`src/assets/` 下只有 `icons/`），头像改为**昵称 FNV-1a 稳定哈希 → `--cl-avatar-1~8` 底色 + 昵称首字**派生，故该文档"⚠️ 8 个 SVG 来源与许可尚未登记"的风险**已因不引入这些文件而消失**，但文档仍按原风险表述挂着 | 与 E-007 同类：后续会话可能**重复引入 DiceBear / 自制子集 / 第三套图标**，或据"尚未落地"判断 CR-055 批次②③④ 待做。**处置须同时改 §1「已采用」事实基线、§2 判定枚举（🟡 → ✅）与 §0 读表警示三处**，并顺带更正 [docs/README.md](README.md) §2 该行内"截至建档仅批次① 已落地"的同源句子——属另一份文档的独立改动，**[CR-062](变更日志/变更台账.md) 只登记不处置** |
| E-010 | 变更台账 §1 存在**悬空锚点**（点击跳转落空） | 本版为校验链接完整性跑了一次机器扫描（正则取 `<a id="cr-NNN">` 与 `](#cr-NNN)` 两个集合做差集）：§2 有锚点小节 **53 个**、§1 与其它处引用 **57 处**、**悬空 4 处** = `#cr-036`、`#cr-043`、`#cr-056`、`#cr-057`——这四个编号在 §1 各有一行且行内链接到 §2，但 **§2 从未建过对应小节**（`grep '^### CR-'` 的标题序列里 036 / 043 / 056 / 057 直接缺席）。成因可查：[CR-056](变更日志/变更台账.md) / [CR-057](变更日志/变更台账.md) 登记时即被并发会话记为"本会话不代建其明细小节"（台账 §4 v1.53 与 v1.54 各有一句），此后无人补建；CR-036 / CR-043 属增量 PRD 草案号，登记时同样只写了 §1 行 | 后果是**在最该可追溯的地方点不动**：从 §1 汇总表想跳到某条明细会静默回到页首，跨会话核对历史时容易误判"这条没记录"。**处置二选一**（均为纯文档、低风险）：① 在 §2 补四个"轻量登记：见 §1 行 + 指针"小节并加锚点（推荐，与 [CR-029](变更日志/变更台账.md) 的一行式精神一致，且能让 §1 链接全部落地）；② 把这四处 §1 链接改指该 CR 的 §4 版本条目或实施方案文件。**本条只登记不处置**（[CR-062](变更日志/变更台账.md) 的范围是界面规范）；同批发现 [接口契约 README](设计/接口契约/README.md) 与 [docs/README.md](README.md) 的相对链接**零失效**（230 / 272 条），故本条只涉及台账内部锚点〔**2026-09-19 复跑注（[CR-071](变更日志/变更台账.md#cr-071) 收尾轮）**：本条所列四处**原样仍在**（`#cr-036` / `#cr-043` / `#cr-056` / `#cr-057`），复跑同一扫描（75 个 md 文件、相对链接 + `<a id>` 与标题 slug 两套锚点合集）**新查出两处本条未列的**：① [CR-059 方案](开发/实施方案/CR-059-修复楼层点赞跨帖子风险.md) 指向本清单 `#bug-001楼层点赞存在跨帖子操作风险`，而该标题实为「BUG-001：…（已修复）」⇒ **锚点少了尾缀，点了落空**（与本条同类，处置同样是"补 `<a id>` 或改链接"，**本批只登记不修**）；② `说明与模板.md` 里的 `#cr-xxx` 是**模板占位符、不是缺陷**，扫描器误报，已排除。**本轮顺手修掉的一处**：[CR-070](变更日志/变更台账.md#cr-070) 轮建 §2 小节时漏了 `<a id="cr-070">`（该轮在六份文档里写了 11 处 `#cr-070` 引用，全部悬空），本轮建 CR-071 小节时**两个锚点一起补上**，故 `#cr-070` / `#cr-071` 自本版起可跳转。**这条重复出现的成因值得记一句**：台账 §2 的锚点是**手写 HTML**，新增小节时没有任何机器步骤会提醒"你还没加 `<a id>`"，而 `mvn verify` 也管不到 markdown ⇒ **只要 §1 行里写了 `[CR-xxx](#cr-xxx)`，就必须同步在 §2 建 `<a id>`，这一条现在只能靠收尾时的链接扫描拦**〕 |
| E-011 | **"仅某角色可调用"这条防线没有机器强制**（CR-066 反证实验实测出的流程缺口；**✅ 已处置，2026-09-19，[CR-068](变更日志/变更台账.md)**——风险本体消失，但"角色取值"那一半仍由 403 用例承担，见本行末注） | 反证做法：把 `AdminModerationController` 两个方法里的 `CurrentUser.requireRole(auth, "ROLE_SUPERADMIN")` 换成 `CurrentUser.requireId(auth)`（即只验登录、不验角色）后，**`ArchitectureGuardTest` 11 例仍全绿**、`EndpointAuthorizationManagerTest` 也全绿——只有 `AdminModerationControllerAuthTest` 的 2 个 403 用例变红。根因是守护规则 G7 的判据是"标了 `@SecurityRequirement` 的方法**必须真的调用 `CurrentUser`**"，而 `requireId` 与 `requireRole` 都是它的合法调用，**规则看不见角色这一层**；框架层（`EndpointAuthorizationManager`）本就只区分"登录 / 未登录"（AGENTS.md「测试约定」已写明）。**受影响的现网端点**：`AdminRosterController`（名册导入）、`AdminModerationController`（内容处置）两个"仅 SUPERADMIN"端点，以及后续任何按 PRD F-ADMIN-001 做的 OPS 端点 | 后果是**权限提升可以静默发生**：一次"顺手把 requireRole 改成 requireId"的重构（或复制粘贴普通用户端点的方法体）能通过全部守护测试与 `mvn verify`，只有在跑到那 2 个控制器用例时才暴露——**而控制器用例是本次新加的，历史上很多受保护端点并没有角色维度的用例**。**候选处置**（择一，均属"改架构守护规则"、须单独登记 CR）：① 给 `ArchitectureGuardTest` 加一条 G9——`module/*/web/Admin*Controller` 的映射方法必须调用 `requireRole`；② 更通用的做法是给 `common/web` 加一个带角色参数的注解（如 `@RequireRole("ROLE_SUPERADMIN")`）并由 AOP 统一裁决，让声明与判定同源，再让守护测试断言"`@SecurityRequirement` 的方法要么调 `requireRole` 要么调 `requireId` 且路径不含 `/admin/`"；③ 最低成本：约定"每个仅超级管理员可调的端点必须有一个 403 用例"并把它写进 `AGENTS.md` 测试约定（**约定不解决静默问题，只解决遗漏问题**）。**本条只登记不处置**（[CR-066](变更日志/变更台账.md#cr-066) 的范围是内容处置本身）<br>✅ **已处置（2026-09-19，[CR-068](变更日志/变更台账.md)）——采纳候选 ① + ③ 两条并行，②留作架构级后续**：**① `ArchitectureGuardTest` 新增 G9**，凡有效路径落在 `/api/v1/admin/**` 的 `module/*/web` 映射方法必须真的调用 `CurrentUser.requireRole`（路径由类级 + 方法级映射注解的显式声明拼接算出；**解析不出的声明形态直接抛异常而非静默放行**、**扫不到任何管理端点也算违规以防空转**）；**③ 约定入档**：AGENTS.md「测试约定」新增一条"每个管理端点必须配一个「角色不足 → 403」用例"。**两条反证复跑均成立**（这是本条闭环的唯一依据，不是"新增了个测试"就算闭环）：**(a)** 把两个处置端点的 `requireRole` 摘成 `requireId` → **G9 与既有 2 个 403 用例同时变红**（处置前只有后者红，即"静默"二字所指）；**(b)** 临时加一个"标了 `@SecurityRequirement`、只调 `requireId`"的 `/api/v1/admin/probe` 探针 → **G7 仍绿、G9 独红**，说明"新端点漏写角色判定"自此自动被拦；两次实验后生产代码当场复原（`sha256sum -c` 三文件全 OK）、复跑 `mvn -o verify` **249 全绿**。⚠️ **两处残余如实标注、不因本条闭环而宣称消失**：**Ⅰ. G9 守"有没有做角色判定"，不守"角色取值得当不当"**——`requireRole(auth, "ROLE_USER")` 仍会通过机器层，那一半只能由端点自带的 403 用例负责（这正是把 ③ 一并写进 AGENTS.md 的理由，两条是互补不是重复）；**Ⅱ. 候选 ②（`@RequireRole` 注解 + 过滤器链裁决，让声明与判定同源）未做**——它要同时改 `common/web` 与 `security/`、属架构级改动，按「实施方案约定」须另开 CR 先写可实施方案，且现 G9 + ③ 已把"静默"这一半堵住，② 的收益只剩"少写一处样板"，性价比不足以在本轮夹带。**受影响的两个现网端点**（`AdminRosterController` 名册导入、`AdminModerationController` 内容处置）实测均在 G9 覆盖内（census 得 3 个方法，与快照 `/api/v1/admin/*` 的 3 个 operation 逐一对上）。单测 **248 → 249**、`openapi.json` 未触碰（零生产代码改动）。 |
| E-012 | **主 PRD §4 非功能表有三格与实测不符**（起草 F-ACC-007 时逐格核对代码核出） | 三格独立可复验：① §4「合规 / 个人信息」行写「**学校 / 专业为选填**」——但 `school` / `major` / `grade` 三列**全仓无任何写入代码**（注册命令体只带 `ticket/email/code/nickname`，`Account.registered()` 不接这三项），本机开发库 **53 个账号三列非空数为 0**（`SELECT sum(x is not null)` 实测），即"选填"二字从未变成过任何一条填入口径，也正是这三列今天既无写入方又无口径的根因；② §4 表下方第 281 行注建议把登录态补为「**JWT 7 天滑动续期**」——实测 `JwtService` 只写 `expiration = now + ttlDays`（`application.yml:76` `ttl-days: 7`），**全仓 `security/` 与 `common/jwt/` grep `续期\|refresh\|renew` 零命中、无刷新端点**，即现网是"7 天绝对过期、期间不续期、第 7 天必然掉线"，**技术方案 §5 那句"滑动续期"同样是文档-代码漂移**；③ §4「安全 / Markdown 渲染」行写「**叠加 CSP 策略**」——实测 `Content-Security-Policy` 在 `backend/src` 与 `frontend/src` **零命中**（无响应头、无 meta 标签），即第二道防线目前不存在 | 后果分三层：**①是需求口径空洞**——评审与测试都会以为"用户能填学校专业"，而实际填不了、也没人负责定"能不能改"（F-ACC-007 草案 §4.3 的三分表正是为补这一格而起，并与该行**正面冲突**，已作为 R-4 请发起人改判）；**②是会写错验收数值**——若照"滑动续期"补进基线，测试会去验一个系统没有的行为（且它同时是"封禁 / 注销后旧令牌仍可用最长 7 天"的真实敞口量级）；**③是安全预期落空**——昵称 / 签名一类纯文本字段一旦允许写入，服务端白名单就是**唯一**防线（见 E-013）。**本条只登记不处置**：改基线 §4 属需求基线变更、须由发起人批复并另开 CR 并入 PRD 版本演进，本轮（CR-067 草案轮）**零基线改动** |
| E-013 | **昵称可含任意字符且无内容审查覆盖**（现存能力，非仅新需求风险） | 注册侧昵称只有长度约束：`AccountCommands.java:37` 实测为 `@NotBlank @Size(min = 2, max = 32) String nickname`，**无 `@Pattern`**——而同一条命令里的 `studentId` 与 `name` 都带 `@Pattern` 白名单（`\d{9}` / 中文名或外文名），即**本仓早已有字符集白名单的先例，唯独昵称没用**。于是昵称可写入 `<`、`>`、`"`、`'`、反引号、反斜杠、URL、零宽字符（实测 `utf8mb4_0900_ai_ci` 会折叠大小写 / 全角 / 重音，**但不折叠零宽**：`concat('a',U+200B,'a') = 'aa'` 为 0）。昵称经 `nicknamesOf` 输出到帖子列表 / 详情 / 楼层 / 通知**四类响应**的 `authorNickname`。**目前未成为缺陷，靠的是输出侧转义**：Markdown 走 `MarkdownRenderer` 的 jsoup 白名单、正文与昵称不进 `contentHtml`、前端以文本插值渲染——**但这三处没有一处是专为昵称设的**，叠加 E-012 ③ 的 CSP 零实现，防线只存在于"渲染路径恰好没走 HTML" | 风险有两段：**现存段**——一旦将来任何一处把昵称拼进 HTML 形态（富文本签名、导出、分享卡片、通知正文摘要），存储型 XSS 即可成立；且"加微信 / 引流 URL"这类昵称今天已可写入、无人拦。**扩大段**——[F-ACC-007 草案](需求/产品需求文档-个人中心设置.md) 的 007a 若实施，会把昵称从"注册时写一次、永不更改"变成**随时可反复改写**的字段，并新增 `bio`（200 字纯文本）这一整个自由文本出口，而 F-SAFE-001 机审未实现（设计门 **A3-8 未决策**）⇒ **词表 / 敏感内容判定没有可执行判据**。**候选处置**（择一即可先堵住）：① 抽一处共享字符集白名单（含拒零宽与控制字符）同时供注册与编辑使用，草案已按此写（007a 业务规则）；② 把昵称 / 签名纳入机审调用面（依赖 A3-8）；③ 最低成本：在 `AGENTS.md` 测试约定里加一句"自由文本字段进任何 HTML 形态前必须有服务端净化断言"。**本条只登记不处置**，处置随 F-ACC-007 的实施轮一并做〔**2026-09-19 处置注（[CR-071](变更日志/变更台账.md#cr-071)）：候选 ① 已落地、本条不关闭**——`common/validation/FieldRules` 抽出**一份**字符集禁则（`<>` + `\p{Cntrl}` + 四个零宽码位**显式列出**，理由见该类注释：实测 `utf8mb4_0900_ai_ci` 折叠大小写 / 全角却**不折叠零宽**），**注册与编辑两条链路同时引用同一份**，故上面那句"唯独昵称没用"自此过时（原句保留为登记时点快照，`AccountCommands.java:37` 行号也已漂移；`RegisterCommand.nickname` 现在带 `pattern`，可由契约快照核对）。**为什么不关闭本条，三处仍未动**：**(a) 候选 ② 机审未做**——敏感词 / 辱骂 / 引流文案零覆盖（依赖 A3-8 决策），⚠️ **禁则字符集 ≠ 机审**，别把"挡住了 `<`"读成"挡住了内容风险"；**(b) 候选 ③ 未做**——"自由文本进任何 HTML 形态前必须有服务端净化断言"这条最低成本约定**尚未写进 `AGENTS.md`「测试约定」**；**(c) "加微信 / 引流 URL 昵称今天已可写入"这半句一字未变**，本轮只禁了尖括号与控制 / 零宽字符，URL 与联系方式仍畅通。**另登记一条与本条相关的新表达极限**：springdoc **不输出可重复的 `@Pattern`**，编辑侧昵称挂了 `TEXT_ALLOWED_PATTERN` + `NON_BLANK_PATTERN` 两条，**契约快照里两条都看不见**（见[接口契约 README](设计/接口契约/README.md) §6.3）⇒ 这条防线如今**只有代码与单测能证明它在**，读快照的会话不要以为"契约没写就没有规则"、也不要以为"契约写了才有效"〕
| E-014 | **"大模型 / embedding 服务商"是一条从未被登记过的决策项**（七份增量 PRD 评审时核出） | 四份 AI 草案（CR-033 / 037 / 038 / 039）全部或部分依赖外部模型能力，但：① [部署与机审选型决策材料](评审/部署与机审选型决策材料.md) 只有两个决策点（部署形态 `:18`、机审服务商阿里云 vs 腾讯云 `:40`），**不覆盖 AI 服务商**；② [技术方案](设计/技术方案.md) 全文 grep `LLM\|大模型\|embedding\|向量\|module/ai` **零命中**，ADR-001~012 无一条涉及；③ CR-033 §9 自己写下"视需要新增 ADR-013"但**从未写**；④ `application.yml` 无任何 `campuslink.ai.*` 键，`ProductionSafetyGuard`（[CR-063](变更日志/变更台账.md#cr-063)）自然也不校验其凭据 | **本条只登记不处置**。⚠️ **一处易被读歪的地方先钉住**：这条缺口**不是**"上 A3-8 就顺带解决了"——A3-8 判的是**内容机审**（进路），AI 服务商是**外发 UGC**（出路），二者风险模型不同（后者另需隐私政策文本支撑，而隐私政策全仓不存在）。要真做 AI 功能，须先落：服务商与密钥注入 + 成本上限 → ADR-013 → 技术方案 §3 选型行 → `campuslink.ai.*` 并入生产守卫 → 隐私政策。**若只做 CR-033 的纯检索版（MySQL ngram 全文索引），本条不阻塞**——该版不外发任何内容，见[评审纪要](评审/增量PRD评审纪要.md) §1 与 §6 |

## 5. 不应重复误报为现存问题的事项

以下能力本次核对确认已经存在，不再按“未完成”登记：

- `ArchitectureGuardTest`：11 个架构守护用例已通过；
- 路径级鉴权：`EndpointAuthorizationManager` 已替代 `permitAll()` 兜底；
- Markdown XSS 过滤：服务端 `MarkdownRenderer` 有回归测试；
- 前端代码高亮：`highlight.js` 已按需加载，`v-highlight` 已接入帖子详情和楼层；
- 全库时间戳时区修复：CR-051 已完成；
- Sprint 3 已完成的采纳、点赞收藏、站内搜索、通知中心、热榜后端；
- 前端构建和后端 187 个现有测试当前均通过。〔**2026-09-18 增补**：CR-060 后单测基线为 **192 个**、CR-063 后为 **208 个**、[CR-065](变更日志/变更台账.md#cr-065) 后为 **220 个**（帖子删除：适配器 3 + 收藏读侧 2 + 应用层 5 + 控制器 2），`mvn -o verify` 均全绿。 **2026-09-18 再注（v1.7）：[CR-066](变更日志/变更台账.md#cr-066) 后为 248 个**（内容处置：用例层 11 + 适配器 6 + 鉴权 7 + 通知与摘要 4），`mvn -o verify` 全绿、三组反证各自变红后按 sha256 复原。**〔2026-09-18 再注（v1.6）：[CR-064](变更日志/变更台账.md#cr-064) + [CR-065](变更日志/变更台账.md#cr-065) 已由发起人合并提交为 `8216843` 并推送 origin/main，本清单里"git 凭证待提交"类表述自此消解；⚠️ 但 `openapi.json` 快照的**「提交后核对」仍未执行**（须跑已提交源码重抓比对），所以"契约与已提交代码一致"这一条**目前是断言、不是实测**。〕〕〔**2026-09-19 再注（v1.10）：⚠️ 上句"「提交后核对」仍未执行、目前是断言"已不成立**——[CR-066](变更日志/变更台账.md#cr-066) + [CR-068](变更日志/变更台账.md) 随 `f58ef73` 提交后，本轮自该**已提交树** `mvn -o package` 重建、临时端口 **8097** 重抓，与归档 `openapi.json` **逐字节一致**（68470 字节 / `sha256 28327bc89910d9c1…19266cfb`）、`git diff` 为空 ⇒ **v1.11 + v1.12 两轮一并闭环，"契约与已提交代码一致"自此是实测**（原句一字保留，它正是"快照无机器校验时，敞口能在文档里挂多久"的证据）。**但不改变本条定性**：`openapi.json` **至今无任何机器校验**，快照漂移仍只能靠人跑重抓 + 看 diff 拦，"CI 起栈比对"这一敞口原样留存（见台账 v1.72）。〕〔**2026-09-19 再注（v1.9）：[CR-068](变更日志/变更台账.md) 后为 249 个**（新增 1 例＝`ArchitectureGuardTest` 的 G9「`/api/v1/admin/**` 必须调 `requireRole`」），`mvn -o verify` 全绿；本轮**零生产代码改动**，两支反证（摘 `requireRole`、加 admin 探针）各自变红后按 `sha256sum -c` 复原并复跑全绿，`openapi.json` 未重抓（前后 sha256 一致、mtime 停在 CR-066 的 09-18 22:32）〕

## 6. 建议处理顺序

1. ~~继续修复 `BUG-002`，补“已下架回复、已删除回复”测试，避免继续积累脏互动数据。~~ ✅ **已由 CR-060 于 2026-09-18 完成**（适配器 3 例 + 应用层 2 例零副作用断言 + 反证实验 + 真机取证 17/17，见 §3.1 BUG-002）——**下一项建议执行为第 2 条（生产安全配置启动即校验）**。〔**2026-09-18 再注**：第 2 条已随 [CR-063](变更日志/变更台账.md#cr-063) 完成，本句的"下一项"自该日起为**第 3 / 第 4 条**〕
2. 把生产安全配置改为启动即校验，确保固定验证码、学籍旁路和开发密钥不能误带入共享或生产环境。〔**✅ 已完成（2026-09-18，[CR-063](变更日志/变更台账.md#cr-063)）**——按本条写出方案、经发起人批复"按方案八步实施"后落地：`prod` profile + `ProductionSafetyGuard`（**BFPP**）+ 守护测试 **16 例**，**单测 192 → 208 全绿、反证 6/6 皆红、prod 真机 A/B/C 三轮按预期、dev 零回归、契约零 diff**；方案原估"约 9 例 / 约 201"被实测抬高，两条被推翻的方案假设见 §3.1 BUG-003 实施结果注。上一版记录：〔2026-09-18 进展：🟡 方案已备、未实施——已按本条写出可实施方案并登记 [CR-063](变更日志/变更台账.md)（含 `prod` profile、启动守卫、约 9 例守护测试与三轮真机取证口径），**现停在"等发起人确认后动代码"**；确认后按 §3.1 BUG-003 加注里的验证口径推进，完成再回置本条。〕〕
3. 明确 P0 范围：内容机审、举报处置、管理员后台、账号注销、帖子删除、个人主页、标签闭环；每项先补实施方案和接口契约。〔**2026-09-18 注**：七项中**"帖子删除"已按本条走完"先方案、后契约"两步**（[实施方案](开发/实施方案/CR-065-帖子删除.md) + 契约快照 v1.11 的 `DELETE /posts/{id}`），**但只完成后端一半**，前端入口未做——余下六项仍未启动，本条整体不勾销。〔**2026-09-18 再注（v1.6）**：七项中的**"举报处置"一支按本条走完第一步**——[CR-066](变更日志/变更台账.md#cr-066) 实施方案已写好（内容处置「下架 / 恢复」），**现停在"等发起人确认后动代码"**、接口契约侧本轮零改动（快照仍 v1.11），故本条**第二步（接口契约）未完成、本条整体仍不勾销**〕〔**2026-09-18 实施结果（v1.7）**："举报处置"这一支的**第一步与第二步都已完成**——发起人批复"确认方案，开始实施 CR-066"后按八步落地，契约快照升至 v1.12（+2 受保护 operation / +1 schema `ModerationCommand` / 22 → 24 操作），详见[方案 §7](开发/实施方案/CR-066-内容处置下架与恢复.md)。⚠️ **本条仍不勾销**：七项里"内容机审 / 运营后台（处置台 UI）/ 账号注销 / 个人主页 / 标签闭环"五项未动，而"举报处置"里的**举报与工单两件事也未动**（本轮只做了链尾的处置），"帖子删除"仍缺前端入口〕〔**2026-09-19 进展注（v1.8）**：七项里的**"个人主页"自此有了一份相邻但不同的草案**——[增量 PRD：个人中心与账号设置（F-ACC-007）](需求/产品需求文档-个人中心设置.md)（预留 CR-067，**待发起人评审、通过前不得开工**）。⚠️ **它不等于 F-003 的个人主页本体**：草案只覆盖**本人视角**（资料编辑写入口、我的帖子 / 我的回帖、设置页与注销入口），**他人视角时间线、头像、获赞与被采纳统计一律不认领**（仍归 F-ACC-002 与 CR-036 草案），故**本条与 F-003 都不因这份草案移动状态**。它真正的增量有三处：给"资料编辑"**第一次立了功能号**、定了一张**字段可信度三分表**（学校不给填、年级走推导、专业自报未核验），并把 F-ACC-003 注销所缺的前置（能写 `nickname` 的定向 UPDATE 链路）摆上台面〕〕
4. 补首页“最新 / 热门”切换，并让前端 `listPosts` 显式传 `sort`。
5. 获取真实名册后执行 `bypass=false` 的注册、重复注册、导入和异常回滚全链路回归。
6. 增加最小集成测试：Spring 上下文启动、Flyway、Mapper 扫描、SecurityFilterChain、MySQL / Redis 真实连接；再接入覆盖率和静态扫描。
7. 完成桌面逐页走查和移动端重新排期；补齐新页面截图，并关闭 A3-6。
8. ~~清理文档状态漂移，并处理 CR-057 未跟踪方案与当前工作区改动。~~ 🟡 **部分完成（2026-09-18）**：**E-007 已由 [CR-062](变更日志/变更台账.md) 处置**（界面规范 4 处 `highlight.js` 陈旧表述回填，升 v1.8）、**CR-057 作废方案已经发起人同意删除**（E-008）、工作区在 `8b37194` / `5ad0ee8` 两次提交后收口过一次；**但本条不整体勾销**——同族漂移又发现一例 **E-009**（[前端界面素材选型](设计/UI规范/前端界面素材选型.md) 的 CR-055 批次标记过时，含字体上游与头像实现两处实质差异），**且它暴露的是流程缺口而非单份文档问题**：只要"方案里列了文档落点、实施时只做代码"这个模式不被堵，同类漂移还会再出现。建议下一步除修 E-009 外，把"**CR 方案 §3 列出的文档落点须在收尾时逐项核对是否真改了**"并入收尾约定（改 `AGENTS.md`，属流程侧、须登记 CR）。

## 7. 本次审计边界

本次完成了仓库代码、配置、测试、前端路由 / 页面、PRD、技术方案、变更台账、下一步行动和流程偏离记录的静态核对，并执行了后端单测和前端构建。未在本次会话中重新启动完整 MySQL / Redis / Spring / Vite 栈，也未重新执行浏览器逐页人工走查；因此第 2.2 节的真机与视觉问题仍需后续运行态验证。
